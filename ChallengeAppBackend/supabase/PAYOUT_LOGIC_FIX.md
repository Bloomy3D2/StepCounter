# Исправление логики расчета выигрыша

## ❌ Проблема в старой логике

**Было:**
```sql
payout = prize_pool / количество_победителей
```

**Проблема:** Призовой фонд делился между всеми победителями, что означало:
- Если 1 победитель → получал весь призовой фонд (больше чем 85% от входа)
- Если много победителей → каждый получал меньше

## ✅ Новая логика (исправлено)

**Стало:**
```sql
payout = entry_fee * 0.85  -- 85% от СВОЕГО входа
```

**Преимущества:**
- Каждый победитель получает **85% от СВОЕГО входа**, независимо от количества победителей
- Справедливо для всех
- Работает одинаково для личных и массовых челленджей

## 📊 Примеры

### Пример 1: Один победитель
- Вход: 2,000₽
- Выигрыш: 2,000₽ × 0.85 = **1,700₽** ✅

### Пример 2: Несколько победителей
- Пользователь 1 (вход 2,000₽) → выигрыш: **1,700₽**
- Пользователь 2 (вход 2,000₽) → выигрыш: **1,700₽**
- Пользователь 3 (вход 2,000₽) → выигрыш: **1,700₽**
- **Каждый получает свою долю независимо!**

### Пример 3: Разные входные платы
- Пользователь 1 (вход 2,000₽) → выигрыш: **1,700₽**
- Пользователь 2 (вход 1,000₽) → выигрыш: **850₽**
- Каждый получает 85% от своего входа

## 🔄 Логика для проигравших

**Не изменилась:**
- Проигравший теряет **все деньги** (100% от входа)
- Деньги не возвращаются
- Статус: `is_failed = true`

## 📝 Что нужно сделать

1. Выполнить SQL из `fix-payout-calculation.sql` в Supabase SQL Editor
2. Проверить, что функция `complete_day` обновлена
3. Протестировать на реальных данных

## ⚠️ Важно

- Призовой фонд (`prize_pool`) все еще рассчитывается как сумма всех 85% от входов
- Но теперь он используется только для отображения, не для расчета выигрыша
- Если все выиграют → сумма выигрышей = призовой фонд
- Если выиграют меньше → разница остается у вас (дополнительный доход)
