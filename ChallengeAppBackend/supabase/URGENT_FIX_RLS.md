# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: RLS политики

## ❌ ПРОБЛЕМА

**В БД нет записей с `is_failed = true`**, потому что:
1. SQL функция `fail_challenge` не может обновить данные (возможно, из-за условий WHERE)
2. Edge Function не может обновить данные через Supabase client, потому что **НЕТ RLS политики для UPDATE**

**Результат:** Проваленные челленджи не сохраняются в БД, поэтому они "исчезают" после обновления.

## ✅ РЕШЕНИЕ

### Шаг 1: Создать RLS политики для UPDATE и INSERT

**Выполните SQL из `FIX_RLS_POLICIES.sql` в Supabase SQL Editor:**

```sql
-- Создаем политику для INSERT
CREATE POLICY "Users can insert own challenges"
    ON public.user_challenges FOR INSERT
    WITH CHECK (auth.uid() = user_id);

-- Создаем политику для UPDATE
CREATE POLICY "Users can update own challenges"
    ON public.user_challenges FOR UPDATE
    USING (auth.uid() = user_id)
    WITH CHECK (auth.uid() = user_id);
```

### Шаг 2: Проверить, что политики созданы

```sql
SELECT 
    policyname,
    cmd,
    qual,
    with_check
FROM pg_policies
WHERE tablename = 'user_challenges'
ORDER BY cmd, policyname;
```

**Должно быть 3 политики:**
- `Users can view own challenges` (SELECT)
- `Users can insert own challenges` (INSERT) ← **НОВАЯ**
- `Users can update own challenges` (UPDATE) ← **НОВАЯ**

### Шаг 3: Протестировать провал челленджа

1. Провалите челлендж в приложении
2. Проверьте логи Edge Function в Supabase Dashboard
3. Выполните SQL запрос:
   ```sql
   SELECT id, challenge_id, is_active, is_completed, is_failed, failed_at
   FROM public.user_challenges
   WHERE is_failed = true;
   ```
4. **Теперь должна быть хотя бы одна запись с `is_failed = true`**

### Шаг 4: Если проблема останется

Если после создания политик проблема останется, проверьте:

1. **Логи Edge Function:**
   - Должно быть "✅ Verified: Challenge correctly marked as failed"
   - Или "✅ Verified: Direct update worked correctly" (если сработал fallback)

2. **SQL функция `fail_challenge`:**
   - Выполните тестовый вызов функции вручную:
   ```sql
   SELECT public.fail_challenge(
       'USER_ID_HERE'::UUID,  -- Замените на реальный user_id
       2::BIGINT              -- Замените на реальный challenge_id
   );
   ```
   - Проверьте, что функция возвращает `{"success": true, "rows_updated": 1}`

3. **Проверьте условия WHERE в SQL функции:**
   - Функция ищет записи с `is_completed = false AND is_failed = false`
   - Если запись уже имеет `is_failed = true`, функция не найдет её и не обновит

---

## 🔍 ДИАГНОСТИКА

### Если в БД нет записей с `is_failed = true`:

1. **Проверьте, что SQL функция вызывается:**
   - Смотрите логи Edge Function
   - Должно быть "✅ fail_challenge RPC success"

2. **Проверьте, что SQL функция обновляет данные:**
   - В логах должно быть "rows_updated: 1"
   - Если "rows_updated: 0", значит WHERE условие не нашло запись

3. **Проверьте, что fallback работает:**
   - В логах должно быть "🔄 Attempting direct update via Supabase client as fallback..."
   - После создания RLS политики для UPDATE, fallback должен работать

### Если записи есть, но приложение их не видит:

1. **Проверьте RLS политику для SELECT:**
   - Должна быть "Users can view own challenges"
   - Условие: `auth.uid() = user_id`

2. **Проверьте логи приложения:**
   - Должно быть "🔍 SupabaseManager.getUserChallenges: Raw response - ... isFailed=true"
   - Если `isFailed=false`, значит данные с сервера неправильные

---

## 📝 ЧТО ИЗМЕНИЛОСЬ

### В SQL:
- ✅ Добавлены RLS политики для INSERT и UPDATE на `user_challenges`
- ✅ Обновлен `schema.sql` для включения этих политик

### В Edge Function:
- ✅ Добавлен fallback для прямого обновления через Supabase client
- ✅ Добавлена проверка, что данные действительно обновились

### В Swift коде:
- ✅ Добавлено логирование сырого JSON ответа
- ✅ Добавлена критическая проверка неправильных данных

---

**Дата создания:** 2026-01-23  
**Статус:** 🚨 СРОЧНО - Выполнить немедленно  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
