# 📋 Оставшиеся задачи

## ❗ Критичные (нужно сделать):

### 1. **Добавить новые файлы в project.pbxproj**
**Проблема**: Файлы созданы, но не добавлены в проект:
- `StorageManager.swift` (B1200018)
- `AppConstants.swift` (B1200019)  
- `DataCoordinator.swift` (B1200020)

**Решение**: Добавить в project.pbxproj вручную через Xcode или автоматически

---

### 2. **ReviewManager использует UserDefaults напрямую**
**Проблема**: `ReviewManager.swift` все еще использует `UserDefaults.standard` вместо `StorageManager`

**Файл**: `Models/ReviewManager.swift`
**Строки**: 25, 26, 42, 45, 46, 54, 62, 70

**Решение**: Заменить все вызовы на `StorageManager.shared`

---

## ⚠️ Средний приоритет:

### 3. **Hardcoded цвета в RouteMapView**
**Проблема**: Кнопки управления в RouteMapView используют hardcoded цвета

**Файл**: `Views/RouteMapView.swift`
**Строки**: 184, 216

**Решение**: Заменить `Color(red: 0.1, green: 0.1, blue: 0.14)` на `cardColor` из ThemeManager

---

### 4. **Hardcoded цвета в специфичных местах**
**Осталось ~18 hardcoded цветов**:
- `ChallengesView.swift:585` - цвета для рейтинга участников (специфичные)
- `ProfileLevelView.swift:207` - градиент для Premium карточки (специфичный)
- `HomeView.swift:437` - цвета для рейтинга (специфичные)
- `ShareManager.swift:62-63` - UIColor для генерации изображений (это нормально)
- `GlassComponents.swift:346` - Preview (это нормально)
- И другие специфичные случаи

**Оценка**: Большинство из них - специфичные дизайнерские решения, можно оставить

---

## 📈 Низкий приоритет (улучшения):

### 5. **Большие View файлы**
- `ChallengesView.swift` - 723 строки
- `AchievementManager.swift` - 711 строк
- `PetView.swift` - 624 строки
- `AchievementsView.swift` - 589 строк

**Решение**: Разделить на подкомпоненты (не критично)

---

### 6. **Отсутствие обработки ошибок**
**Проблема**: Много мест с `try?` без логирования

**Решение**: Создать `ErrorLogger` для централизованного логирования

---

### 7. **Оптимизация computed properties**
**Проблема**: В `HealthManager` много computed properties пересчитываются каждый раз

**Решение**: Добавить кеширование

---

## ✅ Уже выполнено:

- ✅ StorageManager создан
- ✅ AppConstants создан
- ✅ DataCoordinator создан
- ✅ Заменено UserDefaults на StorageManager в основных менеджерах
- ✅ Заменено 58+ hardcoded цветов на ThemeManager
- ✅ Удалены backup файлы

---

## 📊 Итоговая статистика:

- **Критичные задачи**: 2 (добавить файлы в проект, ReviewManager)
- **Средние задачи**: 2 (RouteMapView цвета, специфичные цвета)
- **Низкий приоритет**: 3 (рефакторинг, оптимизация)

**Рекомендация**: Сделать критичные задачи, остальное можно отложить.
