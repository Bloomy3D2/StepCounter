# 🔧 ИНСТРУКЦИЯ: Настройка в Xcode

## 📋 Нужно добавить в проект Xcode

Созданы следующие файлы, которые нужно добавить в проект:

### ✅ 1. Файлы, которые УЖЕ добавлены:
- ✅ `GoalSettingsView.swift` — уже в проекте

### ❌ 2. Файлы, которые НУЖНО добавить:

#### В Models:
- ❌ `ReferralManager.swift`

#### В Views:
- ❌ `InviteFriendsView.swift`
- ❌ `PublicLeaderboardView.swift`

---

## 📝 Как добавить файлы в Xcode

### Вариант 1: Автоматически через Xcode (РЕКОМЕНДУЕТСЯ)

1. **Откройте Xcode**
2. **Выберите проект** в левом навигаторе (StepCounter)
3. **Перейдите в нужную папку:**
   - Для `ReferralManager.swift` → папка **Models**
   - Для `InviteFriendsView.swift` → папка **Views**
   - Для `PublicLeaderboardView.swift` → папка **Views**

4. **Правый клик** на папке → **"Add Files to StepCounter..."**

5. **Выберите файлы:**
   - `StepCounter/Models/ReferralManager.swift`
   - `StepCounter/Views/InviteFriendsView.swift`
   - `StepCounter/Views/PublicLeaderboardView.swift`

6. **ВАЖНО!** Отметьте галочки:
   - ✅ **"Copy items if needed"** (если файлы не в папке проекта)
   - ✅ **"Add to targets: StepCounter"** — ОБЯЗАТЕЛЬНО!

7. Нажмите **"Add"**

---

### Вариант 2: Перетащить файлы (Drag & Drop)

1. **Откройте Finder** и найдите файлы
2. **Перетащите файлы** в нужные папки в Xcode:
   - `ReferralManager.swift` → в папку **Models**
   - `InviteFriendsView.swift` → в папку **Views**
   - `PublicLeaderboardView.swift` → в папку **Views**

3. В диалоге **"Choose options for adding these files"** отметьте:
   - ✅ **"Copy items if needed"**
   - ✅ **"Add to targets: StepCounter"**

4. Нажмите **"Finish"**

---

## ✅ Проверка после добавления

После добавления файлов проверьте:

1. **Откройте файлы** в Xcode — они должны открываться без ошибок
2. **Попробуйте скомпилировать** (⌘+B):
   - Если ошибок нет — всё готово! ✅
   - Если есть ошибки — см. раздел "Решения проблем"

---

## 🔍 Проверка в project.pbxproj

Если вы хотите проверить вручную, что файлы добавлены:

1. Откройте `StepCounter.xcodeproj/project.pbxproj` в текстовом редакторе
2. Найдите строки:
   - `ReferralManager.swift`
   - `InviteFriendsView.swift`
   - `PublicLeaderboardView.swift`

Если эти строки есть — файлы добавлены ✅

---

## 🐛 Решения проблем

### Ошибка: "Cannot find 'ReferralManager' in scope"

**Решение:** 
- Проверьте, что файл `ReferralManager.swift` добавлен в target "StepCounter"
- Выберите файл → Inspector (правая панель) → Target Membership → ✅ StepCounter

### Ошибка: "Cannot find 'InviteFriendsView' in scope"

**Решение:**
- Аналогично проверьте `InviteFriendsView.swift`
- Убедитесь, что файл добавлен в target

### Ошибка компиляции: "No such module"

**Решение:**
- Product → Clean Build Folder (⇧⌘K)
- Product → Build (⌘B)

---

## 📦 Дополнительные настройки (если нужно)

### Если используете Firebase для лидербордов:

1. **Добавьте Firebase SDK** (если еще не добавлен)
2. **Настройте Firebase** в `StepCounterApp.swift`

### Если используете Widgets:

1. **Создайте Widget Extension:**
   - File → New → Target
   - Выберите **"Widget Extension"**
   - Название: `StepCounterWidget`

2. **Добавьте код** в `StepCounterWidget.swift`

---

## ✅ Чек-лист

- [ ] `ReferralManager.swift` добавлен в Models и target
- [ ] `InviteFriendsView.swift` добавлен в Views и target
- [ ] `PublicLeaderboardView.swift` добавлен в Views и target
- [ ] Проект компилируется без ошибок
- [ ] Все импорты работают корректно

---

## 🎯 После добавления

После успешного добавления файлов:

1. ✅ Система приглашений будет работать
2. ✅ Экран "Пригласить друзей" будет доступен из профиля
3. ✅ Публичные лидерборды будут готовы к использованию (нужно интегрировать в навигацию)

**Готово!** 🎉
