# Исправление проблем с оплатой

## ✅ Исправлено

### 1. СБП теперь открывает сайт банка
- Добавлен метод `processSBPPayment` в `PaymentManager`
- При выборе СБП открывается URL в Safari
- Пользователь переходит на сайт для выбора банка

### 2. Улучшена обработка ошибок при вступлении
- Добавлено детальное логирование ошибок
- Улучшены сообщения об ошибках для пользователя
- Разные сообщения для разных типов ошибок

---

## 🔍 Диагностика ошибки "Оплата прошла, но вступление не удалось"

### Возможные причины:

1. **Пользователь уже участвует в челлендже**
   - Проверьте в БД: `SELECT * FROM user_challenges WHERE user_id = 'USER_ID' AND challenge_id = CHALLENGE_ID;`

2. **Челендж не найден или неактивен**
   - Проверьте: `SELECT * FROM challenges WHERE id = CHALLENGE_ID;`
   - Убедитесь, что `is_active = true`

3. **Проблема с SQL функцией `join_challenge`**
   - Убедитесь, что выполнили SQL скрипт `allow-early-registration.sql`
   - Проверьте, что убрана проверка `start_date > NOW()`

4. **Проблема с авторизацией**
   - Проверьте, что пользователь авторизован в Supabase
   - Проверьте RLS политики

---

## 🔧 Что нужно проверить

### 1. Выполнен ли SQL скрипт?

Откройте Supabase Dashboard → SQL Editor и выполните:
```sql
-- Проверьте, что функция обновлена
SELECT pg_get_functiondef(oid) 
FROM pg_proc 
WHERE proname = 'join_challenge';
```

Убедитесь, что в функции **НЕТ** проверки:
```sql
IF v_challenge.start_date > NOW() THEN
    RAISE EXCEPTION 'Challenge has not started yet';
END IF;
```

Если проверка есть, выполните скрипт `allow-early-registration.sql`.

### 2. Проверьте логи в Xcode

При ошибке вступления вы увидите в консоли:
```
🚨 КРИТИЧЕСКАЯ ОШИБКА: Оплата успешна, но вступление не удалось.
🚨 User: USER_ID, Challenge: CHALLENGE_ID
🚨 Error: ERROR_MESSAGE
```

Скопируйте эти логи и проверьте, какая именно ошибка.

### 3. Проверьте статус челленджа в БД

```sql
SELECT id, title, start_date, end_date, is_active, entry_fee
FROM challenges 
WHERE id = YOUR_CHALLENGE_ID;
```

### 4. Проверьте участие пользователя

```sql
SELECT * 
FROM user_challenges 
WHERE user_id = 'YOUR_USER_ID' 
AND challenge_id = YOUR_CHALLENGE_ID;
```

---

## 📱 Как работает СБП сейчас

1. Пользователь выбирает СБП
2. Нажимает "Оплатить через СБП"
3. **Открывается Safari с URL СБП** (новое!)
4. Пользователь выбирает банк и оплачивает
5. Возвращается в приложение
6. Приложение пытается вступить в челлендж

**Для MVP:** После открытия URL приложение ждет 5 секунд и считает оплату успешной.

**Для продакшена:** Нужно использовать webhook или polling для проверки реального статуса платежа.

---

## ⚠️ Важно

### URL СБП в MVP

Сейчас используется тестовый URL:
```
https://qr.nspk.ru/{amount}/{transactionId}
```

**В продакшене** этот URL нужно заменить на реальный от:
- ЮKassa API
- Банковского API (Сбер, Тинькофф, и т.д.)

### Как получить реальный URL СБП

1. **Через ЮKassa:**
   - Создайте платеж через ЮKassa API
   - Получите `confirmation_url` для СБП
   - Откройте этот URL

2. **Через банковский API:**
   - Интегрируйтесь с API банка (Сбер, Тинькофф)
   - Создайте платеж СБП
   - Получите URL для редиректа

---

## 🐛 Если ошибка все еще возникает

1. **Проверьте логи в Xcode:**
   - Откройте консоль (⌘⇧Y)
   - Найдите строки с 🚨
   - Скопируйте полный текст ошибки

2. **Проверьте SQL функцию:**
   - Убедитесь, что выполнили `allow-early-registration.sql`
   - Проверьте, что нет проверки `start_date`

3. **Проверьте авторизацию:**
   - Убедитесь, что пользователь авторизован
   - Проверьте RLS политики в Supabase

4. **Проверьте Edge Function:**
   - Убедитесь, что Edge Function `join-challenge` развернута
   - Проверьте логи Edge Function в Supabase Dashboard

---

## ✅ Итог

✅ **СБП теперь открывает сайт банка** - добавлен редирект через `UIApplication.shared.open`

✅ **Улучшена обработка ошибок** - детальное логирование и понятные сообщения

⚠️ **Для полного исправления ошибки вступления:**
- Выполните SQL скрипт `allow-early-registration.sql`
- Проверьте логи в Xcode для диагностики
- Убедитесь, что Edge Function работает корректно
