# 🔧 Ручное исправление пути к Info.plist

## ❌ Проблема

Ключи не читаются из Info.plist, потому что Build Script использует неправильный путь.

---

## ✅ Решение: Исправить в Xcode

### Вариант 1: Исправить Build Script в Xcode (рекомендуется)

1. **Откройте проект в Xcode**

2. **Target "ChallengeApp" → Build Phases → Run Script**

3. **Найдите строку:**
   ```bash
   PLIST="${TARGET_BUILD_DIR}/${INFOPLIST_PATH}"
   ```

4. **Замените на:**
   ```bash
   # Используем PROCESSED_INFOPLIST_PATH для GENERATE_INFOPLIST_FILE = YES
   if [ -n "${PROCESSED_INFOPLIST_PATH}" ] && [ -f "${PROCESSED_INFOPLIST_PATH}" ]; then
       PLIST="${PROCESSED_INFOPLIST_PATH}"
   else
       # Fallback на обычный путь
       PLIST="${TARGET_BUILD_DIR}/${INFOPLIST_PATH}"
   fi
   ```

5. **Удалите строки создания Info.plist** (они больше не нужны):
   ```bash
   # Удалите эти строки:
   # if [ ! -f "$PLIST" ]; then
   #     echo "⚠️ Info.plist not found at $PLIST, creating..."
   #     mkdir -p "$(dirname "$PLIST")"
   #     /usr/libexec/PlistBuddy -c "Save" "$PLIST" 2>/dev/null || touch "$PLIST"
   # fi
   ```

6. **Сохраните проект (⌘ + S)**

---

### Вариант 2: Использовать INFOPLIST_KEY_* автоматически

Если Build Script не работает, можно полагаться на `INFOPLIST_KEY_*`, которые уже настроены в `.xcconfig` файлах.

**Они должны автоматически попадать в Info.plist!**

Проверьте в Build Settings:
- `INFOPLIST_KEY_YOOKASSA_SHOP_ID` = `1256365` (Debug)
- `INFOPLIST_KEY_YOOKASSA_SECRET_KEY` = `test_...` (Debug)

Если они есть - они должны работать автоматически.

---

## 🧪 Проверка

После исправления:

1. **Пересоберите проект (⌘ + B)**

2. **Проверьте логи сборки:**
   - Должно быть: `🔑 Copying YooKassa keys to Info.plist...`
   - Должно быть: `✅ YooKassa keys copied successfully`

3. **Запустите приложение (⌘ + R)**

4. **Проверьте логи приложения:**
   - Должно быть: `🔑 AppConfig: YooKassa Shop ID from Info.plist: 1256365`

---

## ✅ Итог

**Исправьте путь к Info.plist в Build Script, и ключи начнут работать!**
