# ⚠️ ВАЖНО: Ключи ЮKassa в project.pbxproj

## 📋 Текущая ситуация

Ключи ЮKassa хранятся в **User-Defined Settings** в Build Settings, которые попадают в файл `ChallengeApp.xcodeproj/project.pbxproj`.

**Это означает:**
- ✅ Ключи работают и доступны в runtime
- ⚠️ Ключи попадут в git при коммите `project.pbxproj`

---

## 🔒 Рекомендации по безопасности

### Вариант 1: Не коммитить project.pbxproj (не рекомендуется)

**Проблема:** `project.pbxproj` - критичный файл проекта, его нужно коммитить.

### Вариант 2: Перевыпустить ключи после первого коммита (рекомендуется)

1. После первого коммита в git:
   - Войдите в личный кабинет ЮKassa
   - Перейдите: **"Настройки" → "Ключи API"**
   - **Перевыпустите** `secretKey` (старый ключ будет недействителен)
   - Обновите ключи в Build Settings

2. Старые ключи в git будут недействительны - безопасно

### Вариант 3: Использовать приватный репозиторий

Если репозиторий приватный - ключи в git не критичны, но все равно рекомендуется перевыпустить после первого коммита.

---

## ✅ Текущая настройка

**Debug (тест):**
- `YOOKASSA_SHOP_ID` = `1256365`
- `YOOKASSA_SECRET_KEY` = `test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo`
- `YOOKASSA_TEST_MODE` = `YES`

**Release (продакшен):**
- `YOOKASSA_SHOP_ID` = `1254934`
- `YOOKASSA_SECRET_KEY` = `live_qSdkVXs_uFx88lkZzzyX0DkK-iparwPb6Jm4lM7ArXM`
- `YOOKASSA_TEST_MODE` = `NO`

---

## 🧪 Проверка

После пересборки проекта проверьте логи:
- Должно быть: `YooKassa client initialized in test mode` (для Debug)
- НЕ должно быть: `YooKassa keys not configured`

---

## 📝 Примечание

`.xcconfig` файлы оставлены для будущего использования, но сейчас используются User-Defined Settings, так как они работают напрямую в runtime.
