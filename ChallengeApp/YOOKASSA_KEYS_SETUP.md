# 🔑 Настройка ключей ЮKassa

## ⚠️ ВАЖНО: Безопасность ключей

**НЕ КОММИТЬТЕ ЭТИ КЛЮЧИ В GIT!**

Эти ключи должны быть добавлены только локально или через переменные окружения.

---

## 📋 Полученные ключи

### 🧪 ТЕСТОВЫЙ РЕЖИМ:
```
shop_id = 1256365
secret_key = test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo
```

### 🚀 ПРОДАКШЕН:
```
shop_id = 1254934
secret_key = live_qSdkVXs_uFx88lkZzzyX0DkK-iparwPb6Jm4lM7ArXM
```

---

## ✅ Ключи получены

Теперь у вас есть правильные **`secret_key`** для REST API, которые используются в приложении.

---

## 🔧 Способ 1: Через Info.plist (рекомендуется для разработки)

### Для тестирования:

1. Откройте проект в Xcode
2. Найдите файл `Info.plist` (или создайте через Xcode: Target → Info → Custom iOS Target Properties)
3. Добавьте следующие ключи:

```xml
<key>YOOKASSA_SHOP_ID</key>
<string>1256365</string>

<key>YOOKASSA_SECRET_KEY</key>
<string>test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo</string>

<key>YOOKASSA_TEST_MODE</key>
<true/>
```

### Для продакшена:

```xml
<key>YOOKASSA_SHOP_ID</key>
<string>1254934</string>

<key>YOOKASSA_SECRET_KEY</key>
<string>live_qSdkVXs_uFx88lkZzzyX0DkK-iparwPb6Jm4lM7ArXM</string>

<key>YOOKASSA_TEST_MODE</key>
<false/>
```

**⚠️ ВАЖНО:** Добавьте `Info.plist` в `.gitignore`, если там хранятся секретные ключи!

---

## 🔧 Способ 2: Через переменные окружения (для CI/CD)

### Для тестирования:

```bash
export YOOKASSA_SHOP_ID=1256365
export YOOKASSA_SECRET_KEY=test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo
export YOOKASSA_TEST_MODE=true
```

### Для продакшена:

```bash
export YOOKASSA_SHOP_ID=1254934
export YOOKASSA_SECRET_KEY=live_qSdkVXs_uFx88lkZzzyX0DkK-iparwPb6Jm4lM7ArXM
export YOOKASSA_TEST_MODE=false
```

---

## 🔧 Способ 3: Через Xcode Build Settings (рекомендуется)

1. Откройте проект в Xcode
2. Выберите Target → **Build Settings**
3. Найдите **User-Defined Settings** (или добавьте через `+`)
4. Добавьте:
   - `YOOKASSA_SHOP_ID` = `1256365` (для Debug) / `1254934` (для Release)
   - `YOOKASSA_SECRET_KEY` = `test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo` (для Debug) / `live_qSdkVXs_uFx88lkZzzyX0DkK-iparwPb6Jm4lM7ArXM` (для Release)
   - `YOOKASSA_TEST_MODE` = `YES` (для Debug) / `NO` (для Release)

---

## ✅ Проверка настройки

После добавления ключей:

1. **Запустите приложение**
2. **Проверьте логи** - должно быть:
   ```
   YooKassa client initialized in test mode
   ```
   или
   ```
   YooKassa client initialized in production mode
   ```

3. **Попробуйте создать тестовый платеж** с картой `5555 5555 5555 4444`

---

## 🧪 Тестирование

### Тестовые карты:

- **Успешная оплата**: `5555 5555 5555 4444`
- **Отмена**: `5555 5555 5555 4477`
- **Срок**: любая будущая дата
- **CVC**: любые 3 цифры

---

## 🔒 Безопасность

### ✅ Правильно:
- Хранить ключи в `Info.plist` (не коммитить в git)
- Использовать переменные окружения
- Использовать Xcode Build Settings с разными значениями для Debug/Release

### ❌ НЕПРАВИЛЬНО:
- Хранить ключи в коде
- Коммитить ключи в git
- Публиковать ключи в открытых репозиториях

---

## 📝 Рекомендации

1. **Для разработки**: Используйте тестовые ключи (`test_`)
2. **Для продакшена**: Используйте продакшен ключи (`live_`)
3. **Автоматическое переключение**: Настройте разные ключи для Debug/Release схем
4. **Проверка**: Убедитесь, что тестовый режим работает перед переходом на продакшен

---

## 🆘 Если ключи не работают

1. **Проверьте формат**: Тестовые ключи должны начинаться с `test_`, продакшен с `live_`
2. **Получите `secret_key`**: Если `mobile_sdk_key` не работает, получите `secret_key` из личного кабинета
3. **Проверьте права**: Убедитесь, что ключ имеет права на создание платежей
4. **Проверьте логи**: Смотрите ошибки в консоли Xcode

---

## 📞 Поддержка

Если возникли проблемы:
- Проверьте документацию: https://yookassa.ru/developers/using-api
- Обратитесь в поддержку ЮKassa: support@yookassa.ru
