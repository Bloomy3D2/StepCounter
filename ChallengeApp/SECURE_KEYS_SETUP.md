# 🔒 Безопасная настройка ключей ЮKassa

## ✅ Что сделано

1. ✅ Созданы `.xcconfig` файлы для хранения ключей
2. ✅ Добавлены в `.gitignore` - ключи не попадут в git
3. ✅ Создан шаблон `Config.xcconfig.example` (можно коммитить)

---

## 📁 Структура файлов

```
ChallengeApp/ChallengeApp/
├── Config.xcconfig.example  ← Шаблон (можно коммитить)
├── Config.xcconfig           ← Тестовые ключи (в .gitignore)
└── Config.Release.xcconfig  ← Продакшен ключи (в .gitignore)
```

---

## 🔧 Настройка в Xcode

### Шаг 1: Подключите .xcconfig файлы к проекту

**⚠️ ВАЖНО: Configurations находятся на уровне PROJECT, а не Target!**

1. Откройте проект в Xcode
2. В левой панели выберите **PROJECT "ChallengeApp"** (верхний элемент, синяя иконка, НЕ Target!)
3. Выберите вкладку **"Info"** (не "Build Settings")
4. Прокрутите вниз до раздела **"Configurations"**
5. Для **Debug**: выберите `Config.xcconfig` из выпадающего списка
6. Для **Release**: выберите `Config.Release.xcconfig` из выпадающего списка

**Если не видите Configurations:**
- Убедитесь, что выбрали **PROJECT** (верхний элемент), а не **TARGET**
- Configurations находятся на уровне проекта, не таргета

### Шаг 2: Удалите User-Defined Settings (если были)

1. Перейдите в **"Build Settings"**
2. Найдите **"User-Defined"** секцию
3. Удалите `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`, `YOOKASSA_TEST_MODE`
4. Теперь значения будут браться из `.xcconfig` файлов

---

## ✅ Преимущества этого подхода

1. **Безопасность**: Ключи не попадут в git (файлы в `.gitignore`)
2. **Разделение**: Разные ключи для Debug и Release
3. **Удобство**: Легко менять ключи, не открывая Xcode
4. **Шаблон**: `Config.xcconfig.example` можно коммитить как пример

---

## 🔍 Проверка

После настройки:

1. **Запустите приложение** (Debug схема)
2. **Проверьте логи** - должно быть:
   ```
   YooKassa client initialized in test mode
   ```
3. **Проверьте git status**:
   ```bash
   git status
   ```
   Файлы `Config.xcconfig` и `Config.Release.xcconfig` **НЕ должны** отображаться

---

## 📝 Если файлы уже в git

Если вы случайно закоммитили ключи:

1. **Удалите из истории git**:
   ```bash
   git rm --cached ChallengeApp/ChallengeApp/Config.xcconfig
   git rm --cached ChallengeApp/ChallengeApp/Config.Release.xcconfig
   git commit -m "Remove YooKassa keys from git"
   ```

2. **Если ключи уже в истории** (критично!):
   ```bash
   # Перевыпустите ключи в личном кабинете ЮKassa!
   # Старые ключи скомпрометированы
   ```

---

## 🆘 Альтернатива: Оставить User-Defined Settings

Если предпочитаете использовать User-Defined Settings в Xcode:

1. **Удалите `.xcconfig` файлы** (или не подключайте их)
2. **Используйте User-Defined Settings** как было настроено
3. **⚠️ ВАЖНО**: User-Defined Settings попадают в `project.pbxproj`
4. **Проверьте `project.pbxproj`** - убедитесь, что ключи там есть
5. **Если `project.pbxproj` в git** - ключи попадут в репозиторий!

**Рекомендация**: Используйте `.xcconfig` файлы для безопасности.

---

## ✅ Итог

- ✅ Ключи хранятся в `.xcconfig` файлах
- ✅ Файлы добавлены в `.gitignore`
- ✅ Шаблон `Config.xcconfig.example` можно коммитить
- ✅ Разные ключи для Debug и Release
- ✅ Безопасно для работы в команде
