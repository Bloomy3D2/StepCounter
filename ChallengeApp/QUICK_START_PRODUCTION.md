# ⚡ Быстрый старт для продакшена

## ✅ Что уже готово

1. ✅ Build Script создан: `scripts/copy-yookassa-keys.sh`
2. ✅ Файлы конфигурации созданы (в .gitignore)
3. ✅ Ключи защищены от коммита в git

---

## 🚀 Один шаг до запуска

### Добавьте Build Script в Xcode (2 минуты):

1. Откройте `ChallengeApp.xcodeproj` в Xcode

2. Target "ChallengeApp" → **Build Phases** → **"+"** → **"New Run Script Phase"**

3. Вставьте скрипт:
   ```bash
   "${SRCROOT}/scripts/copy-yookassa-keys.sh"
   ```

4. **Важно:** Снимите галочку **"Run script only when installing"**

5. Перетащите скрипт **ПОСЛЕ** "Copy Bundle Resources"

6. Сохраните (⌘ + S)

---

## 🧪 Проверка

```bash
⌘ + B  # Build
⌘ + R  # Run
```

В логах должно быть:
```
🔑 AppConfig: YooKassa Shop ID from Info.plist: 1256365
```

---

## 🔐 Безопасность

**Все ключи защищены:**
- ✅ Не попадут в git
- ✅ Работают автоматически для Debug/Release
- ✅ Готово для продакшена

---

Подробная инструкция: `PRODUCTION_SETUP_COMPLETE.md`
