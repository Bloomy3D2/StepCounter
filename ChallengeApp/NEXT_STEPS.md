# Следующие шаги после настройки Supabase

## ✅ Что уже сделано:
- [x] Создан проект Supabase
- [x] Применен SQL скрипт (schema.sql)
- [x] Настроены API ключи в SupabaseManager.swift

---

## 📋 Чеклист следующих шагов:

### ШАГ 1: Проверка базы данных ✅

Проверьте в Supabase Dashboard:

1. **Table Editor** → должны быть таблицы:
   - ✅ `users`
   - ✅ `challenges`
   - ✅ `user_challenges`
   - ✅ `completed_days`
   - ✅ `payments`
   - ✅ `challenge_rules`

2. **Database** → **Functions** → должны быть функции:
   - ✅ `join_challenge`
   - ✅ `complete_day`
   - ✅ `fail_challenge`
   - ✅ `get_current_day`
   - ✅ `has_completed_today`

3. **Database** → **Indexes** → должны быть индексы (23 штуки)

---

### ШАГ 2: Добавить тестовые данные 🎯

В **SQL Editor** выполните:

```sql
-- Добавить тестовые челленджи
INSERT INTO public.challenges (
    title, subtitle, icon, duration, entry_fee,
    start_date, end_date, description, is_active
) VALUES 
(
    'Подъём до 7:00',
    '7 дней подряд',
    'sunrise.fill',
    7,
    10.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '8 days',
    'Просыпайся до 7:00 каждый день в течение недели',
    true
),
(
    '10,000 шагов',
    '14 дней подряд',
    'figure.walk',
    14,
    20.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '15 days',
    'Проходи минимум 10,000 шагов каждый день',
    true
),
(
    'Без соцсетей',
    '30 дней',
    'hand.raised.fill',
    30,
    50.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '31 days',
    'Полный отказ от социальных сетей на месяц',
    true
);

-- Добавить правила для челленджей
INSERT INTO public.challenge_rules (challenge_id, rule, order_index)
SELECT id, 'Каждый день отмечай выполнение', 1 FROM public.challenges WHERE title = 'Подъём до 7:00'
UNION ALL
SELECT id, 'Нет отметки = вылет', 2 FROM public.challenges WHERE title = 'Подъём до 7:00'
UNION ALL
SELECT id, 'Деньги не возвращаются', 3 FROM public.challenges WHERE title = 'Подъём до 7:00'
UNION ALL
SELECT id, 'Каждый день отмечай выполнение', 1 FROM public.challenges WHERE title = '10,000 шагов'
UNION ALL
SELECT id, 'Нет отметки = вылет', 2 FROM public.challenges WHERE title = '10,000 шагов'
UNION ALL
SELECT id, 'Деньги не возвращаются', 3 FROM public.challenges WHERE title = '10,000 шагов'
UNION ALL
SELECT id, 'Каждый день отмечай выполнение', 1 FROM public.challenges WHERE title = 'Без соцсетей'
UNION ALL
SELECT id, 'Нет отметки = вылет', 2 FROM public.challenges WHERE title = 'Без соцсетей'
UNION ALL
SELECT id, 'Деньги не возвращаются', 3 FROM public.challenges WHERE title = 'Без соцсетей';
```

Проверьте: **Table Editor** → `challenges` → должно быть 3 челленджа

---

### ШАГ 3: Установить Supabase SDK в Xcode 📦

1. Откройте проект в Xcode
2. **File** → **Add Packages...**
3. Вставьте URL: `https://github.com/supabase/supabase-swift`
4. Выберите версию: **Up to Next Major Version** → `2.0.0`
5. Нажмите **Add Package**
6. Выберите target: **ChallengeApp**
7. Нажмите **Add Package**

Проверьте: В Project Navigator должно появиться `Package Dependencies` → `supabase-swift`

---

### ШАГ 4: Обновить импорты в SupabaseManager.swift 📝

Убедитесь, что в начале файла есть:

```swift
import Foundation
import Supabase
```

---

### ШАГ 5: Интегрировать Supabase в менеджеры 🔄

Нужно обновить существующие менеджеры, чтобы они использовали Supabase вместо локального хранения.

**Файлы для обновления:**
- `ChallengeApp/Managers/ChallengeManager.swift`
- `ChallengeApp/Managers/AuthManager.swift`
- `ChallengeApp/Managers/PaymentManager.swift`

---

### ШАГ 6: Протестировать подключение 🧪

1. Запустите приложение в Xcode
2. Попробуйте зарегистрироваться
3. Проверьте консоль - должно быть:
   ```
   ✅ Supabase client initialized successfully
   ```
4. Проверьте в Supabase Dashboard:
   - **Authentication** → **Users** → должен появиться пользователь
   - **Table Editor** → `users` → должен появиться профиль

---

### ШАГ 7: Протестировать загрузку челленджей 📋

1. После регистрации попробуйте открыть экран с челленджами
2. Должны загрузиться 3 тестовых челленджа
3. Проверьте консоль на ошибки

---

## 🐛 Troubleshooting

### Ошибка: "Module 'Supabase' not found"
**Решение:** Убедитесь, что Supabase SDK установлен (Шаг 3)

### Ошибка: "relation does not exist"
**Решение:** Проверьте, что SQL скрипт выполнен полностью (Шаг 1)

### Ошибка: "permission denied"
**Решение:** Проверьте RLS policies в Supabase Dashboard → Authentication → Policies

### Ошибка: "Unauthorized"
**Решение:** Проверьте, что anon key правильный в SupabaseManager.swift

---

## ✅ Готово к тестированию!

После выполнения всех шагов ваше приложение будет:
- ✅ Подключаться к Supabase
- ✅ Регистрировать пользователей
- ✅ Загружать челленджи из БД
- ✅ Сохранять данные в облаке

**Следующий шаг:** Интегрировать Supabase в менеджеры (я могу помочь с этим!)
