# 🔍 Проверка работы Build Script

## ❌ Проблема

Ключи все еще не читаются из Info.plist после исправления пути.

---

## ✅ Что исправлено

1. ✅ Убраны лишние пробелы в начале строк (синтаксическая ошибка)
2. ✅ Используется `PROCESSED_INFOPLIST_PATH` для правильного пути

---

## 🧪 Проверка работы скрипта

### Шаг 1: Проверьте логи сборки

1. **Пересоберите проект (⌘ + B)**

2. **Откройте Report Navigator (⌘ + 9)**

3. **Найдите последнюю сборку и откройте ее**

4. **Найдите "Run Script" в логах**

5. **Проверьте вывод скрипта:**
   - Должно быть: `🔧 Configuration: Debug`
   - Должно быть: `🔑 Copying YooKassa keys to Info.plist...`
   - Должно быть: `✅ YooKassa keys copied successfully to Info.plist`

### Шаг 2: Если скрипт не выполняется

**Проверьте:**
1. Build Script добавлен в Build Phases?
2. Порядок выполнения правильный? (должен быть ПОСЛЕ Resources)
3. Есть ли ошибки в логах сборки?

### Шаг 3: Если скрипт выполняется, но ключи не попадают

**Проверьте:**
1. Читаются ли ключи из Build Settings?
   - Должно быть: `Shop ID: 1256365`
   - Должно быть: `Secret Key: test_3NzNw2knb5rTf-b...`

2. Правильный ли путь к Info.plist?
   - Проверьте логи: `PLIST=...` должен указывать на существующий файл

---

## 🔧 Альтернативное решение

Если Build Script не работает, можно использовать `INFOPLIST_KEY_*` напрямую:

1. **Проверьте Build Settings:**
   - `INFOPLIST_KEY_YOOKASSA_SHOP_ID` = `1256365` (Debug)
   - `INFOPLIST_KEY_YOOKASSA_SECRET_KEY` = `test_...` (Debug)

2. **Если они есть** - они должны автоматически попадать в Info.plist при `GENERATE_INFOPLIST_FILE = YES`

3. **Проверьте в runtime:**
   ```swift
   print(Bundle.main.object(forInfoDictionaryKey: "YOOKASSA_SHOP_ID"))
   ```

---

## 📝 Следующие шаги

1. Пересоберите проект
2. Проверьте логи сборки
3. Если скрипт выполняется - проверьте путь к Info.plist
4. Если скрипт не выполняется - проверьте настройки Build Phases
