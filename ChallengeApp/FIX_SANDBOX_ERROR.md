# ✅ Исправлена ошибка Sandbox

## ❌ Проблема

**Ошибка:** `Sandbox: bash(50622) deny(1) file-read-data /Users/.../scripts/copy-yookassa-keys.sh`

**Причина:** Xcode не может выполнить внешний скрипт из-за sandbox ограничений безопасности.

---

## ✅ Решение

**Встроил скрипт прямо в Build Phase** - теперь он выполняется напрямую без необходимости читать внешний файл.

### Что изменилось:

1. ✅ Скрипт теперь встроен в `project.pbxproj` (Build Phase)
2. ✅ Не нужно читать внешний файл `scripts/copy-yookassa-keys.sh`
3. ✅ Обходит sandbox ограничения
4. ✅ Безопасность сохранена - ключи все еще не попадут в git

---

## 🧪 Проверка

1. **Пересоберите проект:**
   ```bash
   ⌘ + B
   ```

2. **Проверьте логи сборки:**
   Должно быть:
   ```
   🔧 Configuration: Debug
   🔑 Copying YooKassa keys to Info.plist...
   Shop ID: 1256365
   Mode: test
   ✅ YooKassa keys copied successfully to Info.plist
   ```

3. **Запустите приложение:**
   ```bash
   ⌘ + R
   ```

4. **Проверьте логи приложения:**
   Должно быть:
   ```
   🔑 AppConfig: YooKassa Shop ID from Info.plist: 1256365
   ```

---

## 📝 Примечание

Внешний файл `scripts/copy-yookassa-keys.sh` больше не используется, но оставлен для справки. Можно удалить, если хотите.

---

## ✅ Итог

**Проблема решена:** Скрипт теперь работает без ошибок sandbox.

**Безопасность:** Ключи все еще защищены - они в `.xcconfig` файлах (в `.gitignore`).
