# 💰 Бесплатное подключение оплаты картой и СБП для продакшена

## ✅ Что БЕСПЛАТНО:

### 1. Подключение к ЮKassa
- ✅ **Регистрация** — бесплатно
- ✅ **Верификация** — бесплатно
- ✅ **Получение API ключей** — бесплатно
- ✅ **Подписание договора** — бесплатно
- ✅ **Использование API** — бесплатно
- ✅ **Webhook'и** — бесплатно
- ✅ **Техподдержка** — бесплатно

### 2. Комиссии (только с успешных платежей)
- 💳 **Банковские карты**: 2.8% + 15₽ за транзакцию
- 📱 **СБП**: 0.7% за транзакцию
- 🍎 **Apple Pay**: 2.8% + 15₽ (как карты)

**Важно:** Комиссия списывается автоматически из суммы платежа. Вы получаете: `сумма_платежа - комиссия`

**Пример:**
- Платеж 1000₽ картой: комиссия = 43₽, вы получаете **957₽**
- Платеж 1000₽ через СБП: комиссия = 7₽, вы получаете **993₽**

---

## 🚀 Пошаговая инструкция для продакшена

### Шаг 1: Регистрация в ЮKassa (бесплатно)

1. Перейдите на [https://yookassa.ru](https://yookassa.ru)
2. Нажмите **"Подключиться"**
3. Выберите тип организации:
   - **ИП** (Индивидуальный предприниматель)
   - **ООО** (Общество с ограниченной ответственностью)
   - **Самозанятый** (для некоторых типов бизнеса)

4. Заполните данные:
   - Для ИП: ФИО, ИНН, ОГРНИП, паспорт, банковские реквизиты
   - Для ООО: название, ИНН, ОГРН, данные директора, банковские реквизиты

5. Загрузите документы для верификации:
   - Скан паспорта (для ИП) или документы организации
   - Свидетельство о регистрации
   - Документы, подтверждающие банковские реквизиты

6. Дождитесь проверки (обычно 1-3 рабочих дня)

7. Подпишите договор онлайн в личном кабинете

---

### Шаг 2: Получение API ключей (бесплатно)

1. Войдите в [личный кабинет ЮKassa](https://yookassa.ru/my)

2. Перейдите: **Настройки** → **API** → **Ключи для API**

3. Скопируйте:
   - **shopId** (ID магазина) — например: `123456`
   - **secretKey** (Секретный ключ) — например: `live_AbCdEfGhIjKlMnOpQrStUvWxYz...`

4. **Сохраните ключи в безопасном месте!**

---

### Шаг 3: Настройка ключей в приложении

#### Вариант 1: Через Info.plist (рекомендуется для продакшена)

1. Откройте `ChallengeApp.xcodeproj` в Xcode
2. Найдите файл `Info.plist` (или создайте его)
3. Добавьте ключи:

```xml
<key>YOOKASSA_SHOP_ID</key>
<string>ВАШ_SHOP_ID</string>

<key>YOOKASSA_SECRET_KEY</key>
<string>ВАШ_SECRET_KEY</string>

<key>YOOKASSA_TEST_MODE</key>
<false/>
```

#### Вариант 2: Через Supabase Secrets (для Edge Functions)

1. В Supabase Dashboard: **Settings** → **Secrets**
2. Добавьте:
   - `YOOKASSA_SHOP_ID` = ваш shopId
   - `YOOKASSA_SECRET_KEY` = ваш secretKey

3. Edge Function автоматически загрузит их из переменных окружения

#### Вариант 3: Через переменные окружения (для тестирования)

В Xcode: **Product** → **Scheme** → **Edit Scheme** → **Run** → **Arguments** → **Environment Variables**

```
YOOKASSA_SHOP_ID = ваш_shop_id
YOOKASSA_SECRET_KEY = ваш_secret_key
YOOKASSA_TEST_MODE = false
```

---

### Шаг 4: Развертывание Webhook (бесплатно)

Webhook уже реализован в `ChallengeAppBackend/supabase/edge-functions/payment-webhook/index.ts`

#### 4.1 Разверните Edge Function:

```bash
cd ChallengeAppBackend
supabase functions deploy payment-webhook
```

#### 4.2 Получите URL webhook:

После развертывания вы получите URL вида:
```
https://YOUR_PROJECT.supabase.co/functions/v1/payment-webhook
```

#### 4.3 Настройте webhook в ЮKassa:

1. Войдите в личный кабинет ЮKassa
2. Перейдите: **Настройки** → **HTTP-уведомления**
3. Добавьте URL: `https://YOUR_PROJECT.supabase.co/functions/v1/payment-webhook`
4. Выберите события:
   - ✅ `payment.succeeded` (платеж успешен)
   - ✅ `payment.canceled` (платеж отменен)
   - ✅ `payment.waiting_for_capture` (ожидает подтверждения)

5. Сохраните настройки

---

### Шаг 5: Включение реальных платежей

#### 5.1 В коде приложения:

В `PaymentManager.swift` уже есть проверка:

```swift
var useRealPayments: Bool {
    return UserDefaults.standard.bool(forKey: "useRealPayments")
}
```

#### 5.2 Включите реальные платежи:

**Вариант 1: Через код (для тестирования)**
```swift
UserDefaults.standard.set(true, forKey: "useRealPayments")
```

**Вариант 2: Автоматически (рекомендуется для продакшена)**

Обновите `PaymentManager.swift`:

```swift
var useRealPayments: Bool {
    // Проверяем наличие реальных ключей
    let shopId = AppConfig.yooKassaShopId
    let secretKey = AppConfig.yooKassaSecretKey
    
    // Если ключи не дефолтные, значит настроены реальные
    return shopId != "YOUR_SHOP_ID" && 
           secretKey != "YOUR_SECRET_KEY" &&
           !AppConfig.yooKassaIsTestMode
}
```

---

### Шаг 6: Настройка URL Scheme для возврата в приложение

#### 6.1 В Info.plist:

Убедитесь, что есть настройка:

```xml
<key>CFBundleURLTypes</key>
<array>
    <dict>
        <key>CFBundleURLSchemes</key>
        <array>
            <string>challengeapp</string>
        </array>
    </dict>
</array>
```

#### 6.2 В ChallengeAppApp.swift:

Убедитесь, что есть обработка:

```swift
.onOpenURL { url in
    if url.scheme == "challengeapp" && url.host == "payment" {
        // Пользователь вернулся после оплаты
        Task {
            await checkPaymentStatus()
        }
    }
}
```

---

## 🧪 Тестирование перед продакшеном

### 1. Тестовый режим ЮKassa

1. В личном кабинете включите **"Тестовый режим"**
2. Получите тестовые ключи (начинаются с `test_`)
3. Используйте тестовые карты:
   - Успешная: `5555 5555 5555 4444`
   - Отклоненная: `5555 5555 5555 4477`
   - CVV: любой (например, `123`)
   - Срок: любая будущая дата

### 2. Проверка webhook

1. Создайте тестовый платеж
2. Проверьте логи Edge Function в Supabase Dashboard
3. Убедитесь, что платеж обрабатывается корректно

### 3. Проверка возврата в приложение

1. Создайте платеж через СБП
2. Убедитесь, что после оплаты приложение открывается
3. Проверьте, что челлендж добавляется в список

---

## 📊 Мониторинг платежей

### В личном кабинете ЮKassa:

1. **Платежи** — список всех платежей
2. **Статистика** — аналитика по платежам
3. **Возвраты** — управление возвратами

### В Supabase:

1. Таблица `payments` — все платежи из приложения
2. Логи Edge Function — обработка webhook'ов

---

## 🔒 Безопасность

### ✅ Что уже реализовано:

1. **Токенизация карт** — полные данные карт не хранятся
2. **Проверка подписи webhook'ов** — защита от подделки
3. **HTTPS** — все запросы зашифрованы
4. **RLS политики** — защита данных в Supabase

### ⚠️ Важно:

1. **НЕ коммитьте ключи** в Git
2. **Используйте Secrets** для хранения ключей
3. **Регулярно проверяйте** логи на подозрительную активность
4. **Обновляйте ключи** при компрометации

---

## 💡 Оптимизация комиссий

### 1. Используйте СБП

СБП имеет меньшую комиссию (0.7% вместо 2.8%+15₽):
- На платеже 1000₽: экономия ~36₽
- Рекомендуйте пользователям СБП

### 2. Учитывайте комиссию в цене

Вариант 1: Заложить комиссию в стоимость челленджа
```swift
let entryFee = 1000.0 // Цена для пользователя
let commission = entryFee * 0.028 + 15.0 // Комиссия ЮKassa
let netAmount = entryFee - commission // Вы получите
```

Вариант 2: Добавить комиссию к цене
```swift
let basePrice = 1000.0
let commission = basePrice * 0.028 + 15.0
let entryFee = basePrice + commission // Пользователь платит больше
```

---

## 📋 Чеклист для продакшена

### Регистрация и настройка:
- [ ] Зарегистрировался в ЮKassa
- [ ] Прошел верификацию
- [ ] Получил shopId и secretKey
- [ ] Настроил ключи в приложении (Info.plist или Secrets)
- [ ] Развернул Edge Function для webhook
- [ ] Настроил webhook URL в ЮKassa
- [ ] Включил реальные платежи в коде

### Тестирование:
- [ ] Протестировал с тестовыми ключами
- [ ] Протестировал оплату картой
- [ ] Протестировал оплату через СБП
- [ ] Проверил возврат в приложение после оплаты
- [ ] Проверил обработку webhook'ов
- [ ] Протестировал с небольшой реальной суммой

### Безопасность:
- [ ] Убрал ключи из кода (используются только из Info.plist/Secrets)
- [ ] Проверил, что ключи не в Git
- [ ] Настроил мониторинг платежей
- [ ] Проверил логи на ошибки

---

## 🆘 Решение проблем

### Проблема: "Платеж не проходит"

1. Проверьте ключи в Info.plist
2. Проверьте, что `useRealPayments = true`
3. Проверьте логи в Xcode
4. Проверьте статус платежа в личном кабинете ЮKassa

### Проблема: "Webhook не работает"

1. Проверьте URL webhook в ЮKassa
2. Проверьте логи Edge Function в Supabase
3. Убедитесь, что Edge Function развернута
4. Проверьте переменные окружения (Secrets)

### Проблема: "Не возвращается в приложение"

1. Проверьте URL scheme в Info.plist
2. Проверьте обработку `.onOpenURL` в приложении
3. Проверьте `returnUrl` в запросе платежа

---

## 📞 Поддержка

- **Техподдержка ЮKassa**: [https://yookassa.ru/support](https://yookassa.ru/support)
- **Документация**: [https://yookassa.ru/developers](https://yookassa.ru/developers)
- **Личный кабинет**: [https://yookassa.ru/my](https://yookassa.ru/my)

---

## ✅ Итог

**Подключение к ЮKassa — полностью бесплатно!**

Вы платите только комиссию с успешных платежей:
- Карты: 2.8% + 15₽
- СБП: 0.7%

**Нет:**
- ❌ Абонентской платы
- ❌ Платы за подключение
- ❌ Скрытых платежей
- ❌ Минимальных сумм

**Есть:**
- ✅ Бесплатная регистрация
- ✅ Бесплатная верификация
- ✅ Бесплатные API ключи
- ✅ Бесплатные webhook'и
- ✅ Бесплатная техподдержка

**Готово к продакшену!** 🚀
