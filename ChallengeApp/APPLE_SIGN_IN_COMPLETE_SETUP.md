# ✅ Полная настройка Apple Sign In

## 🎯 Что уже сделано автоматически

1. ✅ Создан файл `ChallengeApp.entitlements` с capability Sign in with Apple
2. ✅ Обновлен `project.pbxproj` для использования entitlements файла
3. ✅ Код Apple Sign In реализован в `AuthManager.swift`

---

## 📋 Что нужно сделать вручную

### Шаг 1: Проверка в Xcode (2 минуты)

1. Откройте проект в Xcode
2. Выберите проект в Navigator (синяя иконка сверху)
3. Выберите target **ChallengeApp**
4. Перейдите на вкладку **Signing & Capabilities**
5. Проверьте, что видите **Sign in with Apple** capability
   - Если НЕ видите → нажмите **+ Capability** → добавьте **Sign in with Apple**

**Готово!** Capability должна быть видна.

---

### Шаг 2: Настройка в Apple Developer Portal (10-15 минут)

#### 2.1. Включить Sign in with Apple в App ID

1. Войдите в [Apple Developer Portal](https://developer.apple.com/account)
2. Перейдите в **Certificates, Identifiers & Profiles**
3. Выберите **Identifiers** → найдите ваш App ID: `com.mrgaliev.app.ChallengeApp`
4. Нажмите на него для редактирования
5. Найдите **Sign in with Apple** и включите ✅
6. Нажмите **Save**

#### 2.2. Создать Service ID

1. В **Identifiers** нажмите **+** (создать новый)
2. Выберите **Services IDs** → **Continue**
3. Заполните:
   - **Description**: `ChallengeApp Apple Sign In`
   - **Identifier**: `com.mrgaliev.app.ChallengeApp.applesignin` (уникальный, можно другой)
4. Нажмите **Continue** → **Register**
5. Включите **Sign in with Apple** ✅
6. Нажмите **Configure** рядом с "Sign in with Apple"

#### 2.3. Настроить Service ID

1. **Primary App ID**: выберите `com.mrgaliev.app.ChallengeApp`
2. **Website URLs**:
   - **Domains and Subdomains**: `qvyxkbeafgarcjjppttd.supabase.co`
   - **Return URLs**: `https://qvyxkbeafgarcjjppttd.supabase.co/auth/v1/callback`
3. Нажмите **Save** → **Continue** → **Register**

**Запомните Service ID** (например: `com.mrgaliev.app.ChallengeApp.applesignin`) - он понадобится для Supabase!

#### 2.4. Создать Key для Client Secret

1. В **Keys** нажмите **+** (создать новый)
2. **Key Name**: `ChallengeApp Apple Sign In Key`
3. Включите **Sign in with Apple** ✅
4. Нажмите **Continue** → **Register**
5. **⚠️ ВАЖНО:** Скачайте ключ (`.p8` файл) - он скачивается только один раз!
6. **Key ID** - скопируйте (понадобится для генерации Client Secret)

**Сохраните:**
- Key ID (например: `ABC123XYZ`)
- Team ID (найдите в правом верхнем углу, например: `9QPGH34SC8`)
- Скачанный `.p8` файл

#### 2.5. Сгенерировать Client Secret

**Вариант 1: Онлайн генератор (проще)**

1. Перейдите на: https://appleid.apple.com/signinwithapple/button/helper
2. Или используйте Node.js скрипт ниже

**Вариант 2: Node.js скрипт**

Создайте файл `generate-secret.js`:

```javascript
const jwt = require('jsonwebtoken');
const fs = require('fs');

// Замените на ваши данные:
const teamId = '9QPGH34SC8'; // Ваш Team ID
const clientId = 'com.mrgaliev.app.ChallengeApp.applesignin'; // Service ID
const keyId = 'ABC123XYZ'; // Key ID из шага 2.4
const privateKeyPath = './AuthKey_ABC123XYZ.p8'; // Путь к скачанному ключу

const privateKey = fs.readFileSync(privateKeyPath);

const token = jwt.sign(
  {
    iss: teamId,
    iat: Math.floor(Date.now() / 1000),
    exp: Math.floor(Date.now() / 1000) + 86400 * 180, // 6 месяцев
    aud: 'https://appleid.apple.com',
    sub: clientId,
  },
  privateKey,
  {
    algorithm: 'ES256',
    keyid: keyId,
  }
);

console.log('Client Secret:');
console.log(token);
console.log('\n⚠️ Сохраните этот токен - он понадобится для Supabase!');
console.log('⚠️ Токен действителен 6 месяцев, затем нужно сгенерировать новый.');
```

Запустите:
```bash
npm install jsonwebtoken
node generate-secret.js
```

**Скопируйте Client Secret** - он понадобится для Supabase!

---

### Шаг 3: Настройка в Supabase (5 минут)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard/project/qvyxkbeafgarcjjppttd
2. Перейдите в **Authentication** → **Providers**
3. Найдите **Apple** и включите его ✅
4. Заполните:
   - **Client ID (Service ID)**: `com.mrgaliev.app.ChallengeApp.applesignin` (ваш Service ID)
   - **Client Secret**: вставьте сгенерированный токен из шага 2.5
5. Нажмите **Save**

**Готово!** Apple Sign In настроен в Supabase.

---

### Шаг 4: Тестирование (5 минут)

1. Откройте проект в Xcode
2. Подключите реальное устройство (iPhone/iPad)
   - ⚠️ Apple Sign In **не работает на симуляторе**!
3. Запустите приложение (Cmd+R)
4. На экране входа нажмите **"Войти через Apple"**
5. Авторизуйтесь через Face ID/Touch ID
6. Проверьте, что вход прошел успешно

**Если работает** → ✅ Готово!

**Если ошибка** → см. раздел "Решение проблем" ниже.

---

## ✅ Чеклист

- [ ] Capability добавлена в Xcode (Signing & Capabilities)
- [ ] Sign in with Apple включен в App ID (Apple Developer Portal)
- [ ] Service ID создан и настроен
- [ ] Key создан и скачан (.p8 файл)
- [ ] Client Secret сгенерирован
- [ ] Apple provider настроен в Supabase
- [ ] Протестирован на реальном устройстве

---

## 🐛 Решение проблем

### Ошибка: "Sign in with Apple недоступен"

**Причина:** Capability не добавлена или не включена в App ID.

**Решение:**
1. Проверьте Xcode → Signing & Capabilities → есть ли "Sign in with Apple"
2. Проверьте Apple Developer Portal → App ID → включен ли "Sign in with Apple"

### Ошибка: "Invalid client" или "Invalid client secret"

**Причина:** Неправильный Service ID или Client Secret в Supabase.

**Решение:**
1. Проверьте Service ID в Supabase (должен совпадать с созданным)
2. Проверьте Client Secret (должен быть свежим, не истекшим)
3. Перегенерируйте Client Secret если нужно

### Ошибка: "The operation couldn't be completed"

**Причина:** Проблема с конфигурацией или устройством.

**Решение:**
1. Убедитесь, что тестируете на **реальном устройстве** (не симулятор)
2. Проверьте, что устройство авторизовано в Apple ID
3. Проверьте настройки Apple ID на устройстве

### Ошибка: "Authorization canceled"

**Причина:** Пользователь отменил авторизацию.

**Решение:** Это нормально, просто попросите пользователя попробовать снова.

---

## 📚 Полезные ссылки

- [Apple Documentation](https://developer.apple.com/sign-in-with-apple/)
- [Supabase Apple Provider](https://supabase.com/docs/guides/auth/social-login/auth-apple)
- [Apple Developer Portal](https://developer.apple.com/account)

---

## 🎉 Готово!

После выполнения всех шагов Apple Sign In будет работать в вашем приложении!

**Время настройки:** ~20-30 минут

---

## 📝 Важные заметки

1. **Client Secret действителен 6 месяцев** - нужно будет обновлять
2. **Тестирование только на реальных устройствах** - симулятор не поддерживает
3. **Service ID должен быть уникальным** - используйте ваш bundle ID + `.applesignin`
4. **Сохраните .p8 файл** - он скачивается только один раз!
