# 🗑️ Как удалить User-Defined Settings в Xcode

## 📝 Пошаговая инструкция

### Способ 1: Через клавиатуру (самый простой)

1. Откройте **Target "ChallengeApp"** → вкладка **"Build Settings"**
2. В поиске вверху введите: `YOOKASSA` (чтобы найти все параметры)
3. Найдите нужный параметр (например, `YOOKASSA_SHOP_ID`)
4. **Кликните на название параметра** (левый столбец, не на значение)
5. Нажмите клавишу **Delete** или **Backspace**
6. Подтвердите удаление, если появится диалог

### Способ 2: Через контекстное меню

1. Найдите параметр в Build Settings
2. **Правый клик** (или Control + клик) на названии параметра
3. Выберите **"Delete"** из меню

### Способ 3: Через Command + Delete

1. Найдите параметр
2. **Кликните на название параметра** (выделится вся строка)
3. Нажмите **⌘ + Delete** (Command + Delete)

---

## 🔍 Если не видите параметры

1. В Build Settings вверху справа нажмите на иконку **"+"**
2. В выпадающем меню выберите **"Show User-Defined Settings Only"**
3. Теперь будут видны только User-Defined параметры

---

## ⚠️ Важные моменты

1. **Кликните именно на название** (левый столбец), а не на значение
2. Если параметр не удаляется - возможно, он используется где-то еще
3. После удаления проверьте, что `.xcconfig` файлы подключены

---

## ✅ Проверка после удаления

1. Запустите приложение
2. Проверьте логи - должно работать с ключами из `.xcconfig`
3. Если не работает - верните User-Defined Settings обратно

---

## 🆘 Если не получается удалить

**Вариант 1: Оставить как есть**
- User-Defined Settings не мешают работе `.xcconfig`
- Можно оставить оба варианта (приоритет у `.xcconfig`)

**Вариант 2: Очистить значения**
- Вместо удаления просто очистите значения (оставьте пустыми)
- `.xcconfig` файлы будут использоваться

**Вариант 3: Редактировать project.pbxproj вручную**
- Откройте `ChallengeApp.xcodeproj/project.pbxproj` в текстовом редакторе
- Найдите строки с `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`, `YOOKASSA_TEST_MODE`
- Удалите соответствующие блоки кода
- Сохраните и откройте проект в Xcode

---

## 💡 Рекомендация

Если удаление не получается - **можно оставить User-Defined Settings**. Они не мешают работе `.xcconfig` файлов, просто будут иметь более низкий приоритет. Главное - `.xcconfig` файлы в `.gitignore`, так что ключи не попадут в git.
