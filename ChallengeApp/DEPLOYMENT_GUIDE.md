# Руководство по развертыванию для E2E тестирования

## ✅ Что уже сделано

1. ✅ **Интеграция AuthManager с Supabase**
   - Анонимный вход работает через Supabase
   - Проверка сессии при запуске приложения
   - Fallback на локальное хранилище

2. ✅ **Интеграция ChallengeManager с Supabase**
   - Загрузка челленджей из Supabase
   - Загрузка челленджей пользователя
   - Синхронизация данных
   - Fallback на локальное хранилище при ошибках

3. ✅ **Обработка ошибок**
   - Try-catch во всех async методах
   - Показ ошибок пользователю
   - Fallback на локальное хранилище

4. ✅ **Индикаторы загрузки**
   - `isLoading` в ChallengeManager
   - ProgressView в ChallengesListView
   - Индикаторы в AuthManager

5. ✅ **Синхронизация данных**
   - Автоматическая синхронизация при открытии экрана
   - Pull-to-refresh
   - Проверка сессии при запуске

6. ✅ **Исправлен joinChallenge**
   - Правильная обработка ответа от Edge Function
   - Обработка ошибок

---

## 🔴 Что нужно сделать СЕЙЧАС

### 1. Развернуть Edge Functions в Supabase

**Это критично для работы вступления в челленджи!**

#### Вариант А: Через Supabase CLI (рекомендуется)

```bash
# 1. Установить Supabase CLI (если еще не установлен)
brew install supabase/tap/supabase

# 2. Войти в Supabase
supabase login

# 3. Связать проект (project-ref можно найти в Settings → General)
supabase link --project-ref qvyxkbeafgarcjjppttd

# 4. Перейти в папку с Edge Functions
cd ChallengeAppBackend/supabase/edge-functions

# 5. Развернуть каждую функцию
supabase functions deploy join-challenge
supabase functions deploy complete-day
supabase functions deploy fail-challenge
```

#### Вариант Б: Через Dashboard (если CLI не работает)

1. Откройте **Supabase Dashboard** → **Edge Functions**
2. Нажмите **Create a new function**
3. Название: `join-challenge`
4. Скопируйте код из `ChallengeAppBackend/supabase/edge-functions/join-challenge/index.ts`
5. Нажмите **Deploy**
6. Повторите для `complete-day` и `fail-challenge`

---

### 2. Проверить, что тестовые данные добавлены

1. Откройте **Supabase Dashboard** → **Table Editor** → **challenges**
2. Должно быть **4 челленджа**:
   - Быстрый старт (1 день)
   - Подъём до 7:00 (7 дней)
   - 10,000 шагов (14 дней)
   - Без соцсетей (30 дней)

Если нет - выполните `ChallengeAppBackend/supabase/test-data.sql` в SQL Editor.

---

### 3. Проверить RLS политики

1. Откройте **Supabase Dashboard** → **Authentication** → **Policies**
2. Убедитесь, что для таблиц `users`, `challenges`, `user_challenges` есть политики:
   - Пользователи могут читать свои данные
   - Пользователи могут создавать свои записи
   - Все могут читать активные челленджи

(Политики должны быть в `schema.sql`, если их нет - выполните schema.sql заново)

---

## 🧪 Тестирование

### Сценарий 1: Регистрация и вход

1. Запустите приложение
2. Пройдите onboarding
3. Нажмите "Продолжить без аккаунта"
4. ✅ Должен создаться анонимный пользователь в Supabase
5. Проверьте в **Supabase Dashboard** → **Authentication** → **Users**

### Сценарий 2: Просмотр челленджей

1. Откройте экран "Активные челленджи"
2. ✅ Должны загрузиться 4 челленджа из Supabase
3. ✅ Должен появиться индикатор загрузки
4. Потяните вниз для обновления (pull-to-refresh)
5. ✅ Данные должны обновиться

### Сценарий 3: Вступление в челлендж

1. Выберите любой челлендж
2. Нажмите "Принять вызов"
3. Нажмите "Оплатить и войти"
4. ✅ Должно произойти вступление в челлендж

**Если Edge Functions не развернуты:**
- ❌ Будет ошибка при вступлении
- ✅ Приложение упадет на локальное хранилище
- ✅ Челлендж все равно добавится локально

**Если Edge Functions развернуты:**
- ✅ Челлендж добавится в Supabase
- ✅ Баланс пользователя обновится
- ✅ Статистика челленджа обновится

### Сценарий 4: Выполнение дня

1. Откройте активный челлендж
2. Нажмите "Я выполнил сегодня"
3. ✅ День должен отметиться

**Если Edge Functions не развернуты:**
- ✅ Работает локально

**Если Edge Functions развернуты:**
- ✅ Обновляется в Supabase
- ✅ Проверяется автоматически на провал

### Сценарий 5: Провал челленджа

1. Откройте активный челлендж
2. Нажмите "Я не выполнил" (2 раза)
3. ✅ Челлендж должен провалиться

---

## 📊 Проверка в Supabase Dashboard

После тестирования проверьте:

1. **Authentication → Users**
   - Должен быть создан анонимный пользователь

2. **Table Editor → users**
   - Должен быть профиль пользователя

3. **Table Editor → user_challenges**
   - Должны быть записи о вступлении в челленджи

4. **Table Editor → completed_days**
   - Должны быть записи о выполненных днях

5. **Edge Functions → Logs**
   - Должны быть логи вызовов функций (если развернуты)

---

## ⚠️ Известные ограничения

1. **Apple/Google Sign In** - пока использует анонимный вход
   - Для полной интеграции нужна настройка в Apple Developer Console и Supabase

2. **Платежи** - только симуляция
   - Для реальных платежей нужна интеграция с Apple Pay / Google Pay

3. **Edge Functions** - если не развернуты, работает локально
   - Приложение не упадет, но данные не синхронизируются

4. **Офлайн режим** - частично работает
   - Данные кэшируются локально
   - При отсутствии сети используется локальное хранилище

---

## 🚀 Готово к тестированию!

После развертывания Edge Functions приложение готово к полноценному E2E тестированию.

**Основные функции работают:**
- ✅ Регистрация/Вход
- ✅ Просмотр челленджей
- ✅ Вступление в челлендж (если Edge Functions развернуты)
- ✅ Выполнение дня
- ✅ Провал челленджа
- ✅ Просмотр прогресса
- ✅ Синхронизация данных

**Следующие шаги:**
1. Развернуть Edge Functions
2. Протестировать все сценарии
3. Исправить найденные баги
4. Добавить реальные платежи (если нужно)
5. Настроить Apple/Google Sign In (если нужно)
