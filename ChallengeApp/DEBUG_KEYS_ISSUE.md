# 🔍 Диагностика проблемы с ключами

## ❌ Текущая проблема

Ключи не читаются из Info.plist:
```
⚠️ AppConfig: YooKassa Shop ID not found, using default
⚠️ AppConfig: YooKassa Secret Key not found, using default
```

---

## 🔍 Шаги диагностики

### Шаг 1: Проверьте логи сборки

1. **Пересоберите проект (⌘ + B)**
2. **Откройте Report Navigator (⌘ + 9)**
3. **Найдите последнюю сборку**
4. **Найдите "Run Script" в логах**

**Что искать:**
- Выполняется ли скрипт вообще?
- Есть ли ошибки в скрипте?
- Выводит ли скрипт: `🔧 Configuration: Debug`?
- Выводит ли скрипт: `🔑 Copying YooKassa keys...`?

### Шаг 2: Проверьте Build Settings

1. **Target "ChallengeApp" → Build Settings**
2. **Найдите `INFOPLIST_KEY_YOOKASSA_SHOP_ID`**
3. **Проверьте значения:**
   - Debug: должно быть `1256365`
   - Release: должно быть `1254934`

**Если значения есть** - они должны автоматически попадать в Info.plist!

### Шаг 3: Проверьте Info.plist в runtime

Добавьте временный код в `AppConfig.swift` для отладки:

```swift
// В начале yooKassaShopId:
let allKeys = Bundle.main.infoDictionary?.keys ?? []
print("🔍 All Info.plist keys: \(allKeys)")
print("🔍 YOOKASSA_SHOP_ID: \(Bundle.main.object(forInfoDictionaryKey: "YOOKASSA_SHOP_ID"))")
```

---

## ✅ Возможные решения

### Решение 1: Исправить Build Script вручную

Если скрипт не выполняется из-за синтаксической ошибки:

1. **Откройте Xcode**
2. **Target "ChallengeApp" → Build Phases → Run Script**
3. **Убедитесь, что нет лишних пробелов в начале строк**
4. **Проверьте синтаксис bash**

### Решение 2: Использовать INFOPLIST_KEY_* автоматически

Если `INFOPLIST_KEY_*` настроены в Build Settings, они должны работать автоматически!

**Проверьте:**
- Значения есть в Build Settings?
- `GENERATE_INFOPLIST_FILE = YES`?
- Если да - они должны попадать в Info.plist автоматически

### Решение 3: Упростить - читать из Build Settings напрямую

Можно читать ключи напрямую из переменных окружения во время сборки, но это сложнее.

---

## 📝 Следующие шаги

1. **Проверьте логи сборки** - выполняется ли скрипт?
2. **Проверьте Build Settings** - есть ли `INFOPLIST_KEY_*`?
3. **Добавьте отладочный код** - что в Info.plist?
4. **Сообщите результаты** - что нашли?
