# ✅ Финальное решение проблемы с ключами

## ❌ Проблема

Ключи не читаются из Info.plist:
```
⚠️ AppConfig: YooKassa Shop ID not found, using default
⚠️ AppConfig: YooKassa Secret Key not found, using default
```

---

## ✅ Решение: Использовать INFOPLIST_KEY_* напрямую

**INFOPLIST_KEY_* уже настроены в .xcconfig файлах!** Они должны автоматически попадать в Info.plist при `GENERATE_INFOPLIST_FILE = YES`.

### Проверьте:

1. **Откройте Build Settings в Xcode**
2. **Найдите `INFOPLIST_KEY_YOOKASSA_SHOP_ID`**
3. **Убедитесь, что значения есть для Debug и Release**

Если значения есть - они должны автоматически попадать в Info.plist!

---

## 🔧 Альтернатива: Упростить Build Script

Если Build Script не работает, можно использовать более простой подход - читать ключи напрямую из Build Settings через переменные окружения в runtime.

Но сначала проверьте логи сборки - возможно Build Script выполняется, но использует неправильный путь.

---

## 🧪 Проверка

1. **Пересоберите проект (⌘ + B)**
2. **Проверьте логи сборки (⌘ + 9):**
   - Найдите "Run Script"
   - Должны быть логи: `🔧 Configuration: Debug`

3. **Если скрипт выполняется, но ключи не попадают:**
   - Проверьте путь к Info.plist
   - Используйте `PROCESSED_INFOPLIST_PATH` вместо `${TARGET_BUILD_DIR}/${INFOPLIST_PATH}`

4. **Если скрипт не выполняется:**
   - Проверьте, что он добавлен в Build Phases
   - Проверьте порядок выполнения (должен быть ПОСЛЕ Resources)

---

## 📝 Текущее состояние

- ✅ `.xcconfig` файлы созданы и подключены
- ✅ `INFOPLIST_KEY_*` настроены в .xcconfig
- ✅ Build Script добавлен
- ❓ Путь к Info.plist может быть неправильным

**Следующий шаг:** Проверьте логи сборки и путь к Info.plist.
