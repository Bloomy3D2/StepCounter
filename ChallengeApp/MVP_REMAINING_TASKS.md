# Что осталось реализовать для MVP (LAYER 1 — CORE)

## 📋 Анализ текущего состояния

### ✅ УЖЕ РЕАЛИЗОВАНО:

#### 1.1. Splash/Entry экран ✅
- ✅ `SplashView.swift` — реализован с анимацией
- ✅ Темная тема
- ✅ Кнопка "Начать" — белая, заметная
- ✅ Слоган "Ставка на себя"
- ✅ Навигация через `onStart` callback

#### 1.2. Quick Start экран ✅
- ✅ `QuickStartView.swift` — создан
- ✅ Показ параметров челленджа (1 день, 299₽, категория)
- ✅ Кнопка "Начать за 299 ₽"
- ✅ Кнопка "Выбрать другой"
- ✅ Обработка ошибок
- ✅ Loading state
- ✅ Поиск существующего челленджа (не создает новый)

#### 1.3. Честная серия (базовая версия) ✅
- ✅ Поле `honestStreak: Int` в `User.swift`
- ✅ Поле `honest_streak` в БД (предположительно)
- ✅ Метод `incrementHonestStreak()` в `SupabaseManager`
- ✅ Метод `resetHonestStreak()` в `SupabaseManager`
- ✅ Метод `updateHonestStreak()` в `ChallengeManager`
- ✅ Обновление серии при честном провале (`failChallenge`)
- ✅ Обновление серии при успешном завершении дня (`completeDay`)

#### 1.4. ActiveChallengeView ✅
- ✅ Таймер до конца дня (крупный, заметный)
- ✅ Форматирование времени "HH:MM:SS"
- ✅ Кнопки "Выполнил" и "Провалился" — большие, по центру
- ✅ Обновление каждую секунду через `Timer.publish`
- ✅ Остановка таймера при завершении/провале

#### RootView навигация ✅
- ✅ Убрана проверка `isOnboardingCompleted`
- ✅ Убран `OnboardingView` из навигации
- ✅ После Splash → сразу AuthView или MainTabView

---

## ❌ ЧТО ОСТАЛОСЬ РЕАЛИЗОВАТЬ:

### 1. Отображение честной серии в профиле ⚠️

**Текущее состояние:** Не найдено отображение `honestStreak` в `ProfileView.swift`

**Что нужно:**
- Добавить отображение честной серии в профиле пользователя
- Показывать значение `appState.currentUser?.honestStreak ?? 0`
- Форматирование: "Честная серия: X дней"

**Файлы для изменения:**
- `Views/ProfileView.swift` — добавить секцию с честной серией

**Пример реализации:**
```swift
// В ProfileView, в секции с информацией о пользователе
HStack {
    Text("Честная серия")
        .foregroundColor(.white.opacity(0.7))
    Spacer()
    Text("\(appState.currentUser?.honestStreak ?? 0) дней")
        .fontWeight(.semibold)
        .foregroundColor(.white)
}
```

---

### 2. Проверка навигации после Quick Start ⚠️

**Текущее состояние:** `QuickStartView` открывает `PaymentView`, но нужно проверить:
- После успешной оплаты пользователь должен попасть в активный челлендж
- Навигация должна работать корректно

**Что нужно проверить:**
- После успешной оплаты в `PaymentView` → автоматический переход к `ActiveChallengeView`
- Если челлендж не найден → переход к списку челленджей

**Файлы для проверки:**
- `Views/PaymentView.swift` — логика после успешной оплаты
- `Views/QuickStartView.swift` — обработка закрытия `PaymentView`

---

### 3. Проверка обновления честной серии в UI ⚠️

**Текущее состояние:** 
- Честная серия обновляется в БД через `incrementHonestStreak()`
- Но локальный `appState.currentUser` может не обновляться сразу

**Что нужно:**
- После обновления честной серии вызывать `appState.refreshUser()` для обновления UI
- Или обновлять локально в `updateHonestStreak()`

**Файлы для изменения:**
- `Managers/ChallengeManager.swift` — метод `updateHonestStreak()` — добавить обновление `appState.currentUser`

**Пример:**
```swift
// В updateHonestStreak после успешного increment
Task { @MainActor in
    if var user = appState.currentUser, user.id == userId {
        user.honestStreak = newStreak
        appState.setUser(user)
    }
}
```

---

### 4. Проверка SQL миграции для честной серии ⚠️

**Текущее состояние:** Не проверено наличие SQL миграции

**Что нужно проверить:**
- Существует ли SQL файл для добавления поля `honest_streak` в таблицу `users`
- Существует ли SQL функция `increment_honest_streak()`
- Существует ли SQL функция `reset_honest_streak()`

**Файлы для проверки:**
- `ChallengeAppBackend/supabase/add-honest-streak.sql` (если существует)
- Или создать новый SQL файл с миграцией

**Пример SQL:**
```sql
-- Добавить поле честной серии в users
ALTER TABLE public.users 
ADD COLUMN IF NOT EXISTS honest_streak INTEGER DEFAULT 0 NOT NULL;

-- Индекс для быстрого поиска по серии
CREATE INDEX IF NOT EXISTS idx_users_honest_streak 
ON public.users(honest_streak DESC);

-- Функция для увеличения серии
CREATE OR REPLACE FUNCTION increment_honest_streak(p_user_id UUID)
RETURNS INTEGER AS $$
DECLARE
    v_new_streak INTEGER;
BEGIN
    UPDATE public.users
    SET honest_streak = honest_streak + 1
    WHERE id = p_user_id
    RETURNING honest_streak INTO v_new_streak;
    
    RETURN COALESCE(v_new_streak, 0);
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Функция для сброса серии
CREATE OR REPLACE FUNCTION reset_honest_streak(p_user_id UUID)
RETURNS INTEGER AS $$
DECLARE
    v_new_streak INTEGER := 0;
BEGIN
    UPDATE public.users
    SET honest_streak = 0
    WHERE id = p_user_id
    RETURNING honest_streak INTO v_new_streak;
    
    RETURN COALESCE(v_new_streak, 0);
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

---

## 📊 ИТОГОВЫЙ ЧЕКЛИСТ MVP:

### Обязательные задачи (для завершения MVP):

- [x] **1. Отображение честной серии в профиле** ✅
  - ✅ Добавлена секция в `ProfileView.swift`
  - ✅ Показывается значение `honestStreak` с иконкой пламени
  - ✅ Оформление: оранжевый фон с иконкой
  
- [x] **2. Обновление честной серии в UI после изменений** ✅
  - ✅ Добавлен `NotificationCenter` для уведомления об обновлении
  - ✅ `ProfileView` слушает уведомление и вызывает `appState.refreshUser()`
  - ✅ UI обновляется автоматически при изменении честной серии

- [x] **3. Проверка SQL миграции для честной серии** ✅
  - ✅ SQL файл существует: `ChallengeAppBackend/supabase/add-honest-streak.sql`
  - ✅ Функция `increment_honest_streak()` реализована
  - ✅ Функция `reset_honest_streak()` реализована
  - ✅ Индекс `idx_users_honest_streak` создан
  - ⚠️ **Требуется применить миграцию в Supabase SQL Editor**

- [ ] **4. Тестирование полного flow**
  - Splash → Auth → Quick Start → Payment → Active Challenge
  - Проверка обновления честной серии при провале/завершении
  - Проверка отображения честной серии в профиле

### Опциональные улучшения (не блокируют MVP):

- [ ] Улучшение навигации после Quick Start
- [ ] Добавление анимации при обновлении честной серии
- [ ] Добавление подсказок о значении честной серии

---

## 🎯 ПРИОРИТЕТЫ:

1. **Высокий приоритет:**
   - Отображение честной серии в профиле
   - Обновление UI после изменения честной серии

2. **Средний приоритет:**
   - Проверка SQL миграции
   - Тестирование полного flow

3. **Низкий приоритет:**
   - Улучшения UX (анимации, подсказки)

---

**Статус MVP:** ~85% готовности

**Основные блоки реализованы, осталось:**
- Отображение честной серии в UI
- Синхронизация UI с данными
- Финальное тестирование
