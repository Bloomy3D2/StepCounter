# Чеклист готовности к E2E тестированию

## ✅ Текущий статус

### Что уже готово:
- ✅ Все экраны UI реализованы
- ✅ Навигация между экранами работает
- ✅ Локальное хранилище (UserDefaults) работает
- ✅ Supabase SDK установлен и настроен
- ✅ Базовая структура SupabaseManager создана

### Что нужно сделать:

---

## 🔴 КРИТИЧНО (для работы приложения)

### 1. Интеграция Supabase с менеджерами

**Проблема:** `AuthManager` и `ChallengeManager` используют локальное хранилище, а не Supabase.

**Решение:**

#### 1.1. Интегрировать AuthManager с SupabaseManager

**Файл:** `ChallengeApp/Managers/AuthManager.swift`

Нужно заменить симуляцию на реальные вызовы:

```swift
// Вместо симуляции:
func signInWithApple() async -> User? {
    // ... симуляция
}

// Использовать:
func signInWithApple() async -> User? {
    await MainActor.run {
        isAuthenticating = true
        lastError = nil
    }
    
    do {
        // Реальная интеграция с Apple Sign In
        // Получить token от Apple
        // let token = ... 
        let user = try await SupabaseManager.shared.signInWithApple(token: token)
        await MainActor.run {
            isAuthenticating = false
        }
        return user
    } catch {
        await MainActor.run {
        isAuthenticating = false
        lastError = error.localizedDescription
        return nil
    }
}
```

#### 1.2. Интегрировать ChallengeManager с SupabaseManager

**Файл:** `ChallengeApp/Managers/ChallengeManager.swift`

Добавить методы для загрузки из Supabase:

```swift
func loadChallengesFromSupabase() async {
    do {
        let challenges = try await SupabaseManager.shared.getChallenges()
        await MainActor.run {
            self.availableChallenges = challenges
        }
    } catch {
        print("Error loading challenges: \(error)")
        // Fallback на локальные данные
        loadChallenges()
    }
}

func loadUserChallengesFromSupabase() async {
    guard let userId = appState.currentUser?.id else { return }
    
    do {
        let userChallenges = try await SupabaseManager.shared.getUserChallenges()
        await MainActor.run {
            self.userChallenges = userChallenges
        }
    } catch {
        print("Error loading user challenges: \(error)")
    }
}
```

---

### 2. Развернуть Edge Functions в Supabase

**Проблема:** `joinChallenge`, `completeDay`, `failChallenge` бросают `NotImplemented`.

**Решение:**

#### 2.1. Установить Supabase CLI

```bash
# macOS
brew install supabase/tap/supabase

# Или через npm
npm install -g supabase
```

#### 2.2. Войти и связать проект

```bash
# Войти
supabase login

# Связать проект (найти project-ref в Settings → General)
supabase link --project-ref qvyxkbeafgarcjjppttd
```

#### 2.3. Деплой функций

```bash
cd ChallengeAppBackend/supabase/edge-functions

# Деплой каждой функции
supabase functions deploy join-challenge
supabase functions deploy complete-day
supabase functions deploy fail-challenge
```

**Или через Dashboard:**
1. Supabase Dashboard → Edge Functions
2. Create a new function
3. Скопировать код из `ChallengeAppBackend/supabase/edge-functions/join-challenge/index.ts`
4. Повторить для остальных функций

---

### 3. Добавить тестовые данные в Supabase

**Проблема:** База данных пустая, нет челленджей для тестирования.

**Решение:**

1. Открыть Supabase Dashboard → SQL Editor
2. Выполнить SQL из `ChallengeAppBackend/supabase/test-data.sql`:
   ```sql
   -- Файл уже содержит все необходимые тестовые данные:
   -- - 3 челленджа (Подъём до 7:00, 10,000 шагов, Без соцсетей)
   -- - Правила для каждого челленджа
   ```
3. Или добавить челлендж на 1 день вручную:

```sql
-- Добавить тестовые челленджи
INSERT INTO public.challenges (
    title, subtitle, icon, duration, entry_fee,
    service_fee_percent, start_date, end_date, 
    description, is_active
) VALUES 
(
    'Быстрый старт',
    '1 день',
    'bolt.fill',
    1,
    5.0,
    15.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '2 days',
    'Попробуй себя в однодневном челлендже',
    true
),
(
    'Подъём до 7:00',
    '7 дней подряд',
    'sunrise.fill',
    7,
    10.0,
    15.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '8 days',
    'Просыпайся до 7:00 каждый день в течение недели',
    true
),
(
    '10,000 шагов',
    '14 дней подряд',
    'figure.walk',
    14,
    20.0,
    15.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '15 days',
    'Проходи минимум 10,000 шагов каждый день',
    true
),
(
    'Без соцсетей',
    '30 дней',
    'hand.raised.fill',
    30,
    50.0,
    15.0,
    NOW() + INTERVAL '1 day',
    NOW() + INTERVAL '31 days',
    'Полный отказ от социальных сетей на месяц',
    true
);
```

---

### 4. Настроить авторизацию

#### 4.1. Email/Password (уже включен по умолчанию)

✅ Готово - работает из коробки

#### 4.2. Apple Sign In (опционально)

1. Supabase Dashboard → Authentication → Providers → Apple
2. Включить Apple
3. Настроить Service ID и Secret (см. `SUPABASE_SETUP.md`)

#### 4.3. Google Sign In (опционально)

1. Supabase Dashboard → Authentication → Providers → Google
2. Включить Google
3. Добавить Client ID и Secret

---

## 🟡 ВАЖНО (для полноценной работы)

### 5. Обработка ошибок

**Добавить обработку ошибок во всех async методах:**

```swift
// Пример для ChallengeManager
func joinChallenge(_ challenge: Challenge, userId: String) async throws -> UserChallenge {
    do {
        // Пытаемся через Supabase
        let userChallenge = try await SupabaseManager.shared.joinChallenge(
            challengeId: Int64(challenge.id) ?? 0
        )
        // Обновляем локальное состояние
        await MainActor.run {
            self.userChallenges.append(userChallenge)
            self.activeChallenge = userChallenge
        }
        return userChallenge
    } catch {
        // Fallback на локальное хранилище для MVP
        return joinChallengeLocal(challenge, userId: userId)
    }
}
```

### 6. Синхронизация данных

**Добавить автоматическую синхронизацию:**

```swift
// В ChallengeManager
func syncWithSupabase() async {
    await loadChallengesFromSupabase()
    await loadUserChallengesFromSupabase()
}

// Вызывать при:
// - Запуске приложения
// - Возврате из фона
// - После важных действий (join, complete, fail)
```

### 7. Индикаторы загрузки

**Добавить индикаторы загрузки для всех async операций:**

```swift
@Published var isLoading = false

func loadChallenges() async {
    await MainActor.run { isLoading = true }
    defer { Task { @MainActor in isLoading = false } }
    
    // ... загрузка
}
```

---

## 🟢 ОПЦИОНАЛЬНО (для улучшения UX)

### 8. Офлайн режим

- Кэширование данных локально
- Синхронизация при появлении сети
- Показ статуса подключения

### 9. Push-уведомления

- Напоминания о необходимости отметить выполнение
- Уведомления о старте челленджа
- Уведомления о победе/провале

### 10. Аналитика

- Отслеживание экранов
- Отслеживание действий пользователя
- Метрики конверсии

---

## 📋 Чеклист перед E2E тестированием

### Backend (Supabase):
- [ ] SQL схема применена
- [ ] Edge Functions развернуты
- [ ] Тестовые данные добавлены
- [ ] Авторизация настроена (Email минимум)
- [ ] RLS политики проверены

### iOS App:
- [ ] AuthManager интегрирован с Supabase
- [ ] ChallengeManager интегрирован с Supabase
- [ ] Обработка ошибок добавлена
- [ ] Индикаторы загрузки работают
- [ ] Все экраны открываются без крашей
- [ ] Навигация работает корректно
- [ ] Данные синхронизируются с Supabase

### Тестирование:
- [ ] Регистрация нового пользователя
- [ ] Вход существующего пользователя
- [ ] Просмотр списка челленджей
- [ ] Вступление в челлендж
- [ ] Отметка выполнения дня
- [ ] Просмотр активного челленджа
- [ ] Просмотр прогресса
- [ ] Просмотр профиля

---

## 🚀 Быстрый старт для тестирования

### Вариант 1: С Supabase (рекомендуется)

1. **Применить SQL схему:**
   - Открыть Supabase Dashboard → SQL Editor
   - Скопировать и выполнить `ChallengeAppBackend/supabase/schema.sql`
   - Должно появиться "Success. No rows returned"

2. **Добавить тестовые данные:**
   - В том же SQL Editor выполнить `ChallengeAppBackend/supabase/test-data.sql`
   - Это добавит 3 тестовых челленджа с правилами
   - Для челленджа на 1 день добавить вручную (см. раздел 3)

3. **Развернуть Edge Functions:**
   ```bash
   supabase functions deploy join-challenge
   supabase functions deploy complete-day
   supabase functions deploy fail-challenge
   ```

4. **Интегрировать менеджеры:**
   - Следовать инструкциям из разделов 1.1 и 1.2

5. **Запустить приложение и протестировать**

### Вариант 2: Без Supabase (для быстрого тестирования UI)

1. **Оставить локальное хранилище:**
   - `AuthManager` и `ChallengeManager` уже работают с UserDefaults
   - Все функции работают локально

2. **Тестировать только UI/UX:**
   - Все экраны работают
   - Навигация работает
   - Данные сохраняются локально

3. **Позже интегрировать Supabase:**
   - Когда будет готов backend

---

## 📝 Следующие шаги

1. **Выбрать вариант:** С Supabase или без
2. **Выполнить чеклист** из соответствующего раздела
3. **Протестировать основные сценарии:**
   - Регистрация → Вход → Просмотр челленджей → Вступление → Выполнение
4. **Исправить найденные баги**
5. **Повторить тестирование**

---

## ⚠️ Известные проблемы

1. **Edge Functions не развернуты** - `joinChallenge` бросает `NotImplemented`
2. **Нет интеграции с Supabase** - используется только локальное хранилище
3. **Apple/Google Sign In** - только симуляция, нужна реальная настройка
4. **Платежи** - только симуляция, нужна интеграция с реальным платежным шлюзом

---

## 📞 Поддержка

Если возникнут проблемы:
1. Проверить логи в Xcode Console
2. Проверить логи в Supabase Dashboard → Logs
3. Проверить, что все ключи и URL правильные
4. Убедиться, что Edge Functions развернуты
