# 📝 Как подключить .xcconfig файлы в Xcode

## 🔍 Где найти Configurations

**Configurations находятся на уровне PROJECT, а не Target!**

### Шаг 1: Выберите PROJECT (не Target)

1. В левой панели навигатора (Project Navigator) найдите:
   - **PROJECT** → `ChallengeApp` (верхний элемент, синяя иконка)
   - **TARGETS** → `ChallengeApp` (ниже, под PROJECT)

2. **Выберите PROJECT "ChallengeApp"** (верхний элемент)

### Шаг 2: Откройте вкладку "Info"

1. В главной области Xcode вы увидите вкладки:
   - General
   - Build Settings
   - **Info** ← выберите эту
   - Build Phases
   - Build Rules

2. Выберите вкладку **"Info"**

### Шаг 3: Найдите раздел "Configurations"

1. В разделе "Info" прокрутите вниз
2. Найдите секцию **"Configurations"**
3. Вы увидите список конфигураций:
   - **Debug**
   - **Release**

### Шаг 4: Подключите .xcconfig файлы

1. Для **Debug** конфигурации:
   - Нажмите на выпадающий список (обычно показывает "None" или имя проекта)
   - Выберите `Config.xcconfig`

2. Для **Release** конфигурации:
   - Нажмите на выпадающий список
   - Выберите `Config.Release.xcconfig`

---

## 🖼️ Визуальное описание

```
Xcode Navigator:
├── PROJECT: ChallengeApp  ← ВЫБЕРИТЕ ЭТОТ (верхний элемент)
│   └── Configurations (Debug, Release)
└── TARGETS
    └── ChallengeApp  ← НЕ ЭТОТ (это target)
```

---

## ⚠️ Альтернатива: Оставить User-Defined Settings

Если не можете найти Configurations или предпочитаете использовать User-Defined Settings:

### Вариант 1: Оставить как есть (User-Defined Settings)

**Проблема**: User-Defined Settings попадают в `project.pbxproj`, который коммитится в git.

**Решение**: 
1. Оставьте User-Defined Settings как настроено
2. **Проверьте `project.pbxproj`** - откройте файл и найдите ваши ключи
3. Если ключи там есть и `project.pbxproj` в git - **ключи попадут в репозиторий!**
4. В этом случае лучше **перевыпустить ключи** после первого коммита

### Вариант 2: Использовать переменные окружения

Вместо .xcconfig можно использовать переменные окружения при запуске:

```bash
export YOOKASSA_SHOP_ID=1256365
export YOOKASSA_SECRET_KEY=test_3NzNw2knb5rTf-bMwRMfdACZdtpAmgmRVgdfW9FYToo
export YOOKASSA_TEST_MODE=true
```

Затем запустите приложение из Xcode.

---

## ✅ Рекомендация

**Если User-Defined Settings уже работают:**

1. Оставьте их как есть
2. **Проверьте git status**:
   ```bash
   git status
   git diff ChallengeApp.xcodeproj/project.pbxproj
   ```
3. Если в `project.pbxproj` есть ключи - они попадут в git
4. В этом случае:
   - Либо используйте `.xcconfig` (найдите Configurations на уровне PROJECT)
   - Либо перевыпустите ключи после первого коммита

---

## 🔍 Как проверить, что ключи в project.pbxproj

1. Откройте файл: `ChallengeApp.xcodeproj/project.pbxproj`
2. Найдите в тексте: `YOOKASSA_SECRET_KEY` или `test_3NzNw2knb5rTf`
3. Если нашли - ключи там есть и попадут в git

---

## 📞 Если все еще не видите Configurations

1. Убедитесь, что выбрали **PROJECT** (верхний элемент), а не **TARGET**
2. Проверьте, что открыта вкладка **"Info"**
3. Прокрутите вниз - Configurations обычно внизу страницы
4. Если все еще не видите - возможно, нужно создать конфигурации вручную
