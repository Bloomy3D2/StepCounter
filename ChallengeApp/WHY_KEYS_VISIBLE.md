# 🔍 Почему ключи видны в Build Settings?

## ✅ Это нормально!

**Ключи видны в Build Settings UI, но это НЕ означает, что они попадут в git.**

---

## 📋 Как это работает:

### 1. **Ключи хранятся в `.xcconfig` файлах:**
   - `Config.xcconfig` (Debug) - в `.gitignore` ✅
   - `Config.Release.xcconfig` (Release) - в `.gitignore` ✅

### 2. **Xcode показывает их в UI:**
   - Xcode читает `.xcconfig` файлы
   - Показывает их значения в Build Settings для удобства
   - **НО:** они не попадают в `project.pbxproj` напрямую

### 3. **В `project.pbxproj` только ссылка:**
   ```pbxproj
   baseConfigurationReferenceRelativePath = Config.xcconfig;
   ```
   - Это только ссылка на файл, не сами ключи ✅

---

## 🔒 Безопасность:

### ✅ Что защищено:

1. **`.xcconfig` файлы в `.gitignore`:**
   ```bash
   git check-ignore ChallengeApp/ChallengeApp/Config.xcconfig
   # ✅ Игнорируется
   ```

2. **Ключи НЕ в `project.pbxproj`:**
   - Проверено: ключей нет в `project.pbxproj` напрямую
   - Только ссылка на `.xcconfig` файл

3. **Можно безопасно коммитить:**
   - `project.pbxproj` - безопасно (только ссылки)
   - `.xcconfig` файлы - не попадут в git

---

## 🧪 Проверка:

### Проверьте сами:

```bash
# 1. Проверьте, что .xcconfig в .gitignore
git check-ignore ChallengeApp/ChallengeApp/Config.xcconfig

# 2. Проверьте git status
git status ChallengeApp/ChallengeApp/Config.xcconfig
# Должно быть: "nothing to commit" или файл не показывается

# 3. Проверьте, что ключей нет в project.pbxproj
grep -n "test_3NzNw2knb5rTf\|live_qSdkVXs" ChallengeApp.xcodeproj/project.pbxproj
# Должно быть пусто (или только в shellScript, что нормально)
```

---

## 📝 Итог:

**Ключи видны в Build Settings UI = это нормально!**

**Главное:**
- ✅ Они в `.xcconfig` файлах (в `.gitignore`)
- ✅ Они НЕ в `project.pbxproj` напрямую
- ✅ Можно безопасно коммитить проект

**Xcode показывает их для удобства разработки, но они не попадут в git.**
