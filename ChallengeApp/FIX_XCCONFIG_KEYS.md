# 🔧 Исправление чтения ключей из .xcconfig

## ❌ Проблема

Значения из `.xcconfig` файлов не читаются в runtime. Код получает `YOUR_SHOP_ID` и `YOUR_SECRET_KEY` вместо реальных значений.

**Причина:** `.xcconfig` файлы определяют Build Settings переменные, но они не попадают автоматически в `ProcessInfo.processInfo.environment` или `Bundle.main.object(forInfoDictionaryKey:)`.

---

## ✅ Решение: Использовать User-Defined Settings

К сожалению, `.xcconfig` файлы не работают напрямую для runtime значений в Swift. Нужно использовать **User-Defined Settings** в Build Settings.

**Но:** User-Defined Settings попадают в `project.pbxproj`, который коммитится в git.

**Компромисс:** Использовать User-Defined Settings, но добавить предупреждение в документацию о необходимости перевыпуска ключей после первого коммита.

---

## 🔧 Альтернативное решение: Swift файл с константами

Создать Swift файл, который будет генерироваться из `.xcconfig` через Build Script, или использовать условную компиляцию.

Но самый простой способ - вернуть User-Defined Settings.
