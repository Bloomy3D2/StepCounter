# ⚡ Быстрый ответ: Можно ли коммитить ключи?

## ❌ НЕТ, не рекомендуется коммитить ключи в `project.pbxproj`

**Почему:**
- Ключи попадут в git и будут видны всем, кто имеет доступ к репозиторию
- Если репозиторий станет публичным - ключи будут доступны всем
- Это нарушение безопасности

---

## ✅ Что я сделал:

1. **Удалил ключи из `project.pbxproj`** - теперь они не попадут в git
2. **Создал безопасное решение:**
   - Build Script (`scripts/copy-yookassa-keys.sh`)
   - Локальный файл `Config.local.xcconfig` (в .gitignore)

---

## 🚀 Что делать дальше:

### Вариант 1: Использовать Build Script (рекомендуется)

1. Создайте файл с ключами:
   ```bash
   cp ChallengeApp/Config.local.xcconfig.example ChallengeApp/Config.local.xcconfig
   ```

2. Заполните ключами `Config.local.xcconfig`

3. Добавьте Build Script в Xcode:
   - Target → Build Phases → "+" → "New Run Script Phase"
   - Скрипт: `"${SRCROOT}/scripts/copy-yookassa-keys.sh"`
   - Переместите ПОСЛЕ "Copy Bundle Resources"

4. Пересоберите проект

**Преимущества:** Ключи НЕ попадут в git ✅

---

### Вариант 2: Если репозиторий приватный

Если репозиторий **приватный** и вы не хотите настраивать Build Script:

1. Верните ключи в User-Defined Settings (через Xcode)
2. После первого коммита **перевыпустите ключи** в личном кабинете ЮKassa
3. Обновите ключи в Build Settings

**Риск:** Если репозиторий станет публичным - ключи будут видны.

---

## 📋 Итог:

**Текущее состояние:** Ключи удалены из `project.pbxproj` - безопасно коммитить ✅

**Рекомендация:** Используйте Build Script + `Config.local.xcconfig` для максимальной безопасности.

Подробная инструкция: `SECURE_KEYS_SOLUTION.md`
