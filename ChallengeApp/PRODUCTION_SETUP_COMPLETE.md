# ✅ Настройка для продакшена - ГОТОВО

## 🔒 Безопасность

**Все ключи защищены:**
- ✅ Ключи НЕ попадут в git
- ✅ `.xcconfig` файлы в `.gitignore`
- ✅ `Config.local.xcconfig` в `.gitignore`
- ✅ Build Script не содержит ключей

---

## 📋 Что уже настроено

### 1. ✅ Файлы конфигурации созданы:

- `ChallengeApp/Config.xcconfig` - тестовые ключи (Debug)
- `ChallengeApp/Config.Release.xcconfig` - продакшен ключи (Release)
- `ChallengeApp/Config.local.xcconfig` - локальный fallback (не коммитится)
- `scripts/copy-yookassa-keys.sh` - Build Script

### 2. ✅ Build Script создан:

Скрипт автоматически:
- Читает ключи из Build Settings (.xcconfig файлов)
- Копирует их в Info.plist во время сборки
- Поддерживает Debug и Release конфигурации

### 3. ✅ .gitignore настроен:

Все файлы с ключами добавлены в `.gitignore`

---

## 🚀 Финальный шаг: Добавить Build Script в Xcode

### Инструкция:

1. **Откройте проект в Xcode:**
   ```
   ChallengeApp.xcodeproj
   ```

2. **Выберите Target "ChallengeApp":**
   - В навигаторе выберите проект (самый верхний элемент)
   - Выберите Target **"ChallengeApp"**

3. **Перейдите на вкладку "Build Phases":**
   - Вверху вы увидите вкладки: General, Signing & Capabilities, Resource Tags, Info, Build Settings, Build Phases, Build Rules
   - Выберите **"Build Phases"**

4. **Добавьте Run Script Phase:**
   - Нажмите **"+"** вверху слева
   - Выберите **"New Run Script Phase"**
   - Новый скрипт появится внизу списка

5. **Настройте скрипт:**
   - **Раскройте** новый "Run Script" (нажмите на стрелку слева)
   - В поле "Shell" оставьте `/bin/sh`
   - В поле "Script" вставьте:
     ```bash
     "${SRCROOT}/scripts/copy-yookassa-keys.sh"
     ```
   - **Важно:** Убедитесь, что галочка **"Run script only when installing"** = **НЕ ВКЛЮЧЕНА** (снята)

6. **Переместите скрипт:**
   - Скрипт должен выполняться **ПОСЛЕ** "Copy Bundle Resources"
   - Перетащите "Run Script" выше "Copy Bundle Resources" (или оставьте внизу, но после копирования ресурсов)

7. **Сохраните проект:**
   - Нажмите ⌘ + S

---

## 🧪 Проверка работы

### Шаг 1: Пересоберите проект
```bash
⌘ + B (Build)
```

### Шаг 2: Проверьте логи сборки

В Xcode откройте Report Navigator (⌘ + 9) и найдите последнюю сборку. В логах должно быть:

```
🔧 Configuration: Debug
🔑 Copying YooKassa keys to Info.plist...
   Shop ID: 1256365
   Mode: test
   Secret Key: test_3NzNw2knb5rTf-b...
✅ YooKassa keys copied successfully to Info.plist
```

### Шаг 3: Запустите приложение

1. Запустите в **Debug** режиме (⌘ + R)
2. Проверьте логи приложения - должно быть:
   ```
   🔑 AppConfig: YooKassa Shop ID from Info.plist: 1256365
   🔑 AppConfig: YooKassa Secret Key from Info.plist: test_3NzNw2knb5rTf-b...
   🔑 AppConfig: YooKassa Test Mode from Info.plist (Bool): true
   ```

### Шаг 4: Проверьте Release сборку

1. Выберите схему **"Release"** в Xcode
2. Пересоберите проект (⌘ + B)
3. В логах должно быть:
   ```
   🔧 Configuration: Release
   🔑 Copying YooKassa keys to Info.plist...
   Shop ID: 1254934
   Mode: production
   ```

---

## 🔐 Безопасность для продакшена

### ✅ Что защищено:

1. **Ключи не в git:**
   - `Config.xcconfig` - в `.gitignore` ✅
   - `Config.Release.xcconfig` - в `.gitignore` ✅
   - `Config.local.xcconfig` - в `.gitignore` ✅

2. **Build Script безопасен:**
   - Скрипт не содержит ключей
   - Ключи читаются из Build Settings во время сборки
   - Не попадают в репозиторий

3. **Info.plist генерируется:**
   - Ключи добавляются только во время сборки
   - Не хранятся в исходниках

---

## 📝 Для команды разработчиков

### Каждый разработчик должен:

1. **Скопировать пример файла:**
   ```bash
   cp ChallengeApp/Config.local.xcconfig.example ChallengeApp/Config.local.xcconfig
   ```

2. **Заполнить ключами** (или использовать .xcconfig файлы)

3. **Добавить Build Script** (если еще не добавлен) - см. инструкцию выше

---

## 🎯 Итог

**Все готово для продакшена:**
- ✅ Безопасное хранение ключей
- ✅ Автоматическая настройка для Debug/Release
- ✅ Ключи не попадут в git
- ✅ Работает для всей команды

**Осталось только:** Добавить Build Script в Xcode (см. инструкцию выше)

---

## 🆘 Если что-то не работает

### Проблема: "YooKassa keys not found"

**Решение:**
1. Проверьте, что `.xcconfig` файлы существуют:
   - `ChallengeApp/Config.xcconfig` (Debug)
   - `ChallengeApp/Config.Release.xcconfig` (Release)

2. Проверьте, что они подключены в Build Settings:
   - Target → Build Settings → "Config.xcconfig" должен быть в "Configuration Files"

3. Или создайте `Config.local.xcconfig` как fallback

### Проблема: Build Script не выполняется

**Решение:**
1. Проверьте путь к скрипту: `"${SRCROOT}/scripts/copy-yookassa-keys.sh"`
2. Убедитесь, что скрипт исполняемый: `chmod +x scripts/copy-yookassa-keys.sh`
3. Проверьте логи сборки в Report Navigator
