# Стратегия кэширования для продакшена

## 📊 Текущая реализация

**Текущий TTL:** 5 минут (300 секунд) для всех данных

**Кэшируются:**
- `challenges` - список доступных челленджей
- `userChallenges` - участие пользователя (статусы, прогресс)
- `currentUser` - профиль пользователя (баланс, статистика)

## ✅ Кэширование - ЛУЧШЕ для продакшена

### Преимущества кэширования:

1. **Производительность**
   - Мгновенная загрузка UI (0ms vs 200-1000ms с сервера)
   - Плавная работа без задержек при переключении табов
   - Меньше нагрузки на сервер (экономия на Supabase запросах)

2. **UX (User Experience)**
   - Приложение работает офлайн (показываем кэш)
   - Нет "белых экранов" при загрузке
   - Плавные переходы между экранами

3. **Экономия**
   - Меньше запросов к Supabase = меньше расходы
   - Меньше трафика для пользователя (важно на мобильных)
   - Меньше нагрузка на Edge Functions

4. **Надежность**
   - Работает при временных сбоях сети
   - Fallback на кэш при ошибках сервера

### Проблемы "всегда с сервера":

1. **Задержки**
   - Каждый переход = 200-1000ms ожидания
   - Плохой UX, особенно на медленном интернете

2. **Нагрузка**
   - При 1000 пользователей: 10,000+ запросов/час только на открытие экранов
   - Дорого для Supabase (лимиты на запросы)
   - Медленнее работа Edge Functions

3. **Проблемы с сетью**
   - Приложение не работает офлайн
   - Каждая ошибка сети = плохой UX

## 🎯 Рекомендуемая стратегия для продакшена

### Гибридный подход: разные TTL для разных данных

```swift
// В AppConfig.swift
struct AppConfig {
    // Статичные данные - долгий TTL
    static let challengesCacheTTL: TimeInterval = 1800 // 30 минут
    
    // Критичные данные - очень короткий TTL
    static let userChallengesCacheTTL: TimeInterval = 30 // 30 секунд (статусы критичны)
    static let userBalanceCacheTTL: TimeInterval = 30 // 30 секунд
    
    // Общий TTL для остального
    static let defaultCacheTTL: TimeInterval = 300 // 5 минут
}
```

### Правила инвалидации кэша:

1. **Автоматическая инвалидация при действиях:**
   - После `joinChallenge` → обновить `userChallenges` и `currentUser`
   - После `completeDay` → обновить `userChallenges` и `currentUser`
   - После `failChallenge` → обновить `userChallenges` и `currentUser`
   - После `withdrawBalance` / `depositBalance` → обновить `currentUser`

2. **Оптимистичные обновления:**
   - Сначала обновляем UI локально (мгновенно)
   - Затем синхронизируем с сервером в фоне
   - Если сервер вернул ошибку → откатываем локальные изменения

3. **Фоновая синхронизация:**
   - При открытии приложения → проверяем кэш, затем синхронизируем в фоне
   - При возврате в приложение (из фона) → синхронизация в фоне
   - Периодическая синхронизация каждые 2-3 минуты (если приложение активно)

### Когда НЕ использовать кэш:

1. **Критичные операции:**
   - Перед оплатой → всегда проверять баланс с сервера
   - Перед выводом средств → всегда проверять баланс с сервера
   - При завершении челленджа → проверять статус с сервера

2. **После пользовательских действий:**
   - После нажатия "Выполнил" → обновить с сервера (но сохранить локальные изменения)
   - После нажатия "Провалился" → обновить с сервера
   - После входа в челлендж → обновить с сервера

## 🔧 Рекомендуемые улучшения

### 1. Разные TTL для разных типов данных

```swift
// CacheManager.swift
extension CacheManager {
    func cacheChallenges(_ challenges: [Challenge]) {
        set(challenges, forKey: "challenges", ttl: AppConfig.challengesCacheTTL) // 30 мин
    }
    
    func cacheUserChallenges(_ challenges: [UserChallenge]) {
        set(challenges, forKey: "userChallenges", ttl: AppConfig.userChallengesCacheTTL) // 30 сек
    }
    
    func cacheUser(_ user: User) {
        set(user, forKey: "currentUser", ttl: AppConfig.userBalanceCacheTTL) // 30 сек
    }
}
```

### 2. Умная инвалидация кэша

```swift
// ChallengeManager.swift
func joinChallenge(...) async throws {
    // ... логика входа ...
    
    // Инвалидируем кэш статусов и баланса после успешного входа
    cacheManager.remove("userChallenges") // Статусы изменились (новое участие)
    cacheManager.remove("currentUser")     // Баланс изменился (оплата входа)
    
    // Загружаем свежие данные с сервера
    await loadUserChallengesFromSupabase(forceRefresh: true)
    await appState.refreshUser()
}

func completeDay(...) async throws {
    // ... логика выполнения дня ...
    
    // Инвалидируем кэш статусов и баланса
    cacheManager.remove("userChallenges") // Статусы изменились (день выполнен)
    cacheManager.remove("currentUser")   // Баланс может измениться (выигрыш)
    
    // Синхронизируем с сервером
    // ...
}

func failChallenge(...) async throws {
    // ... логика провала ...
    
    // Инвалидируем кэш статусов
    cacheManager.remove("userChallenges") // Статусы изменились (провален)
    cacheManager.remove("currentUser")   // Для консистентности
    
    // Синхронизируем с сервером
    // ...
}
```

### 3. Фоновая синхронизация

```swift
// В ChallengeAppApp.swift или отдельный BackgroundSyncManager
func startBackgroundSync() {
    Timer.scheduledTimer(withTimeInterval: 120, repeats: true) { _ in
        Task {
            // Синхронизируем только если приложение активно
            if UIApplication.shared.applicationState == .active {
                await challengeManager.syncWithSupabase()
            }
        }
    }
}
```

### 4. Оптимистичные обновления (уже частично реализовано)

- ✅ Локальное обновление UI сразу после действия
- ✅ Синхронизация с сервером в фоне
- ✅ Сохранение локальных изменений при конфликтах

## 📈 Метрики для продакшена

### С кэшированием:
- **Запросы к серверу:** ~50-100 запросов/пользователь/день
- **Время загрузки UI:** 0-50ms (из кэша)
- **UX:** ⭐⭐⭐⭐⭐ (мгновенная работа)

### Без кэширования (всегда с сервера):
- **Запросы к серверу:** ~500-1000 запросов/пользователь/день
- **Время загрузки UI:** 200-1000ms (с сервера)
- **UX:** ⭐⭐⭐ (задержки, белые экраны)

## ⚠️ Важные моменты для продакшена

1. **Баланс пользователя:**
   - Всегда проверять с сервера перед критичными операциями
   - Кэш только для отображения (не для бизнес-логики)

2. **Статусы челленджей:**
   - Локальные изменения имеют приоритет (оптимистичные обновления)
   - Синхронизация с сервером в фоне
   - При конфликте → локальные изменения важнее (пользователь уже видит результат)

3. **Список челленджей:**
   - Можно кэшировать долго (30 минут) - редко меняется
   - Обновлять только при pull-to-refresh или при открытии приложения

4. **Офлайн режим:**
   - Показывать кэш при отсутствии сети
   - Показывать индикатор "офлайн режим"
   - Синхронизировать при восстановлении сети

## 🎯 Итоговая рекомендация

**✅ ИСПОЛЬЗОВАТЬ КЭШИРОВАНИЕ** с умной стратегией:

1. **Очень короткий TTL для критичных данных** (баланс, статусы) - 30 секунд
2. **Длинный TTL для статичных данных** (список челленджей) - 30 минут
3. **Инвалидация кэша при действиях** - всегда обновлять после операций
4. **Оптимистичные обновления** - сначала UI, потом синхронизация
5. **Фоновая синхронизация** - периодическое обновление в фоне

Это даст:
- ⚡ Быстрый UX (мгновенная работа)
- 💰 Экономию на запросах (меньше расходы на Supabase)
- 📱 Работу офлайн (лучший UX)
- 🔒 Надежность (fallback на кэш при ошибках)

**Без кэширования приложение будет медленным и дорогим в масштабе.**
