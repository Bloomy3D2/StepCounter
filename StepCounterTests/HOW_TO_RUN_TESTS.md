# 🚀 КАК ЗАПУСТИТЬ ТЕСТЫ

## 📋 Шаг 1: Создание тестового target (если еще не создан)

### В Xcode:

1. Откройте проект `StepCounter.xcodeproj` в Xcode
2. В меню выберите: `File` → `New` → `Target...`
3. В появившемся окне:
   - Выберите **`Unit Testing Bundle`** (iOS)
   - Нажмите **`Next`**
4. Настройте target:
   - **Product Name:** `StepCounterTests`
   - **Language:** Swift
   - **Target to be Tested:** `StepCounter`
   - Нажмите **`Finish`**
5. Xcode автоматически создаст схему для тестов

## 📋 Шаг 2: Добавление файлов тестов в target

### Автоматически (рекомендуется):

1. В Xcode откройте **Project Navigator** (Cmd + 1)
2. Найдите папку `StepCounterTests`
3. Выберите все файлы тестов (`.swift`)
4. В **File Inspector** (правая панель) найдите секцию **Target Membership**
5. Убедитесь, что галочка стоит напротив **`StepCounterTests`**

### Или вручную:

1. Выберите файл теста в Project Navigator
2. Откройте **File Inspector** (правая панель, Cmd + Option + 1)
3. В секции **Target Membership** поставьте галочку напротив **`StepCounterTests`**
4. Повторите для всех файлов тестов

## 📋 Шаг 3: Настройка импортов

Убедитесь, что в начале каждого тестового файла есть:

```swift
import XCTest
@testable import StepCounter
```

## 🎯 Шаг 4: Запуск тестов

### Способ 1: Через меню (самый простой)

1. В Xcode выберите схему **`StepCounterTests`** в верхней панели (рядом с кнопкой Play)
2. Нажмите **`Cmd + U`** (или `Product` → `Test`)
3. Все тесты запустятся автоматически

### Способ 2: Через Test Navigator

1. Откройте **Test Navigator** (Cmd + 6)
2. Вы увидите все тесты, организованные по файлам
3. Нажмите кнопку **▶️** рядом с:
   - Всей папкой `StepCounterTests` - для запуска всех тестов
   - Конкретным файлом - для запуска тестов в файле
   - Конкретным тестом - для запуска одного теста

### Способ 3: Через редактор кода

1. Откройте любой файл с тестами
2. Нажмите на **▶️** рядом с названием функции теста
3. Или нажмите **Cmd + U** для запуска всех тестов

### Способ 4: Через командную строку

```bash
# Перейдите в директорию проекта
cd /Users/maratgaliev/Downloads/Project/MyProjectGame

# Запустите все тесты
xcodebuild test \
  -scheme StepCounter \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  -only-testing:StepCounterTests

# Или запустите конкретный тест
xcodebuild test \
  -scheme StepCounter \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  -only-testing:StepCounterTests/LevelManagerTests/testAddXPIncreasesTotalXP
```

## 📊 Просмотр результатов

### В Xcode:

1. После запуска тестов откройте **Report Navigator** (Cmd + 9)
2. Выберите последний тест
3. Вы увидите:
   - ✅ Успешные тесты (зеленые галочки)
   - ❌ Провалившиеся тесты (красные крестики)
   - ⚠️ Предупреждения
   - 📊 Время выполнения каждого теста

### В консоли:

Результаты также отображаются в консоли Xcode внизу экрана.

## 🔍 Покрытие кода (Code Coverage)

### Включение Code Coverage:

1. В Xcode: `Product` → `Scheme` → `Edit Scheme...`
2. Выберите **`Test`** в левом меню
3. Откройте вкладку **`Options`**
4. Поставьте галочку **`Code Coverage`**
5. Нажмите **`Close`**

### Просмотр покрытия:

1. Запустите тесты (Cmd + U)
2. Откройте **Report Navigator** (Cmd + 9)
3. Выберите последний тест
4. Откройте вкладку **`Coverage`**
5. Вы увидите процент покрытия для каждого файла

## ⚠️ Решение проблем

### Проблема: "Cannot find 'StepCounter' in scope"

**Решение:**
1. Убедитесь, что в начале файла есть `@testable import StepCounter`
2. Проверьте, что файл добавлен в target `StepCounterTests`
3. Очистите проект: `Product` → `Clean Build Folder` (Shift + Cmd + K)
4. Пересоберите: `Product` → `Build` (Cmd + B)

### Проблема: "No such module 'XCTest'"

**Решение:**
1. Убедитесь, что выбран правильный target (`StepCounterTests`)
2. Проверьте, что в **Build Settings** → **Framework Search Paths** указан путь к XCTest

### Проблема: Тесты не запускаются

**Решение:**
1. Убедитесь, что выбран симулятор iOS (не реальное устройство)
2. Проверьте, что схема `StepCounterTests` активна
3. Попробуйте очистить Derived Data:
   - `Xcode` → `Settings` → `Locations`
   - Нажмите стрелку рядом с **Derived Data**
   - Удалите папку `StepCounter-...`
   - Пересоберите проект

### Проблема: "Main actor isolation" ошибки

**Решение:**
- Убедитесь, что тестовые классы помечены как `@MainActor`
- Используйте `Task { @MainActor in }` для асинхронных операций

## 🎯 Быстрый старт

**Минимальные шаги для запуска:**

1. ✅ Откройте `StepCounter.xcodeproj` в Xcode
2. ✅ Создайте target `StepCounterTests` (если еще нет)
3. ✅ Добавьте все файлы из папки `StepCounterTests` в target
4. ✅ Выберите схему `StepCounterTests`
5. ✅ Нажмите **Cmd + U**

## 📝 Полезные горячие клавиши

- **Cmd + U** - Запустить все тесты
- **Cmd + 6** - Открыть Test Navigator
- **Cmd + 9** - Открыть Report Navigator
- **Cmd + B** - Собрать проект
- **Shift + Cmd + K** - Очистить сборку

## 🎓 Дополнительные ресурсы

- [Apple Testing Documentation](https://developer.apple.com/documentation/xctest)
- [Xcode Testing Guide](https://developer.apple.com/library/archive/documentation/DeveloperTools/Conceptual/testing_with_xcode/)
- [README.md](./README.md) - Общая информация о тестах
