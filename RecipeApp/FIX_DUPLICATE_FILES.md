# 🔧 Исправление ошибки "Multiple commands produce"

## Проблема

Ошибка возникает, когда файлы добавлены в проект несколько раз или есть дубликаты в Build Phases.

## ✅ Решение (3 шага)

### Шаг 1: Найти и удалить дубликаты файлов

1. В Xcode откройте **Project Navigator** (левая панель)
2. Найдите файлы, которые могут быть дублированы (особенно `RecipeApp.swift`)
3. Если видите файл **дважды** в списке:
   - Выберите один из дубликатов
   - Нажмите **Delete** (или правой кнопкой → Delete)
   - Выберите **"Remove Reference"** (НЕ "Move to Trash")
   - Оставьте только один экземпляр файла

### Шаг 2: Проверить Build Phases

1. Выберите проект **RecipeApp** в Project Navigator (самый верхний элемент)
2. Выберите target **RecipeApp**
3. Откройте вкладку **"Build Phases"**
4. Раскройте **"Compile Sources"**
5. Проверьте список файлов:
   - Если видите один и тот же файл **дважды** → удалите дубликат (выделите и нажмите "-")
   - Особенно проверьте `RecipeApp.swift` - он должен быть только один раз

### Шаг 3: Очистить и пересобрать

1. **Product → Clean Build Folder** (Shift + Cmd + K)
2. Закройте Xcode
3. Откройте Xcode снова
4. **Product → Build** (Cmd + B)

---

## 🔍 Альтернативное решение (если не помогло)

### Вариант A: Удалить и добавить файлы заново

1. В Project Navigator удалите все файлы из `Models/` и `Views/` (Remove Reference)
2. Удалите `RecipeApp.swift` (Remove Reference)
3. **Product → Clean Build Folder** (Shift + Cmd + K)
4. Добавьте файлы заново:
   - Правой кнопкой на папку RecipeApp → "Add Files to RecipeApp..."
   - Выберите `Models/`, `Views/` и `RecipeApp.swift`
   - **ВАЖНО:** "Copy items if needed" - **НЕ отмечено**
   - "Create groups" - **отмечено**
   - "Add to targets: RecipeApp" - **отмечено**
5. Нажмите **Add**
6. **Product → Build** (Cmd + B)

### Вариант B: Очистить DerivedData

1. Закройте Xcode
2. Откройте Terminal
3. Выполните:
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/RecipeApp-*
   ```
4. Откройте Xcode
5. **Product → Clean Build Folder** (Shift + Cmd + K)
6. **Product → Build** (Cmd + B)

---

## ✅ Проверка после исправления

После исправления должно быть:

- ✅ В Build Phases → Compile Sources: каждый файл только один раз
- ✅ В Project Navigator: нет дубликатов файлов
- ✅ Build успешен (Build Succeeded)
- ✅ Приложение запускается

---

## 🎯 Частая причина

Обычно это происходит, когда:
- Файлы были добавлены несколько раз
- Файлы скопированы в проект, но оригиналы тоже остались
- `RecipeApp.swift` создан автоматически Xcode И добавлен вручную

**Решение:** Оставить только один экземпляр каждого файла.
