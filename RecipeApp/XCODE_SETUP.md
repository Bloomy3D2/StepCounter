# Инструкция по добавлению RecipeApp в Xcode

## Шаг 1: Создание нового проекта (если нужно)

1. Откройте Xcode
2. File → New → Project
3. Выберите "App" (iOS)
4. Название: `RecipeApp`
5. Interface: SwiftUI
6. Language: Swift
7. Сохраните проект

## Шаг 2: Добавление файлов в проект

### Вариант A: Через Finder

1. Откройте папку `RecipeApp` в Finder
2. В Xcode: File → Add Files to "RecipeApp"...
3. Выберите папку `RecipeApp`
4. Убедитесь, что:
   - ✅ "Copy items if needed" НЕ отмечен (файлы уже на месте)
   - ✅ "Create groups" отмечен
   - ✅ "Add to targets: RecipeApp" отмечен
5. Нажмите "Add"

### Вариант B: Перетаскивание

1. Откройте Finder с папкой `RecipeApp`
2. Перетащите папки `Models` и `Views` в Xcode проект
3. Убедитесь, что "Copy items if needed" НЕ отмечен
4. Убедитесь, что "Add to targets: RecipeApp" отмечен

## Шаг 3: Проверка структуры проекта

Убедитесь, что структура выглядит так:

```
RecipeApp (проект)
├── RecipeApp (target)
│   ├── Models/
│   │   ├── Recipe.swift
│   │   ├── RecipeDatabase.swift
│   │   ├── RecipeMatcher.swift
│   │   ├── LocalAIService.swift
│   │   └── RecipeService.swift
│   ├── Views/
│   │   ├── IngredientsInputView.swift
│   │   ├── RecipeListView.swift
│   │   ├── RecipeDetailView.swift
│   │   └── ContentView.swift
│   └── RecipeApp.swift
```

## Шаг 4: Настройка Info.plist (если нужно)

Если требуется доступ к камере или другим ресурсам, добавьте в `Info.plist`:

```xml
<key>NSCameraUsageDescription</key>
<string>Для фотографирования блюд</string>
```

## Шаг 5: Проверка Target Membership

Для каждого файла убедитесь, что:
1. Выберите файл в Project Navigator
2. Откройте File Inspector (правая панель)
3. В разделе "Target Membership" должна быть галочка у "RecipeApp"

## Шаг 6: Очистка и сборка

1. Product → Clean Build Folder (Shift + Cmd + K)
2. Product → Build (Cmd + B)

## Шаг 7: Запуск

1. Выберите симулятор или устройство
2. Product → Run (Cmd + R)

## Возможные ошибки

### "Cannot find type 'Recipe'"

- Убедитесь, что `Recipe.swift` добавлен в Target
- Проверьте, что файл находится в правильной папке

### "Cannot find 'RecipeService'"

- Убедитесь, что все файлы из папки `Models` добавлены в проект
- Проверьте Target Membership

### "Value of type 'X' has no member 'Y'"

- Убедитесь, что все зависимости между файлами правильно настроены
- Проверьте импорты

## Готово! 🎉

Теперь приложение должно компилироваться и запускаться. Если возникнут проблемы, проверьте:
- Все файлы добавлены в Target
- Нет дублирующихся файлов
- Структура проекта правильная
